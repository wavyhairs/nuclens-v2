# Signal Desk 개편 인계 (2026-08-03)

**이 문서는 `S4(첫 화면 재구성)` 한 칸의 진행 기록이다.** 전체 사다리는
[`PHASE_PLAN.md`](PHASE_PLAN.md) 를 볼 것 — 그쪽이 정본이다.

기준선: `origin/main = 5274c09` · 테스트 봇 223 / 웹 175 · 라이브 배포·자동화 검증 완료.

---

## 1. 위치 — 어디까지 왔나

| S | 명세 | 내용 | 상태 |
|---|---|---|---|
| S0 | — | 브랜치 정리 + 명세 개정 | ⚠️ 브랜치 ✅ / 명세 개정은 **대상 문서 부재로 무효** |
| S1 | §5 승격 | features 결손 | ✅ 코드 완료, 관찰 2주 대기 (`aa3315d`) |
| S2 | Phase 1 | 랭킹 신뢰도 | ⚠️ 2-1만 (`72ce421`) |
| S3 | Phase 2 | 문구 교체 786건 | ❌ 미착수 |
| **S4** | **Phase 3** | **첫 화면 재구성** | 🟡 **여기** |
| S5 | Phase 4 | 민감도 제어 | ❌ `sensitivity_level` 검색 0건 |
| S6 | — | 유머 문구 | ❌ `copy.json` 없음 · S5 선행 |
| S7 | Phase 5 | 사건 추적 | ❌ 이슈 ID 지속성 0건 |

**주의 — 문서 세 개의 "Phase"가 서로 다른 것을 가리킨다:**

| 문서 | 그 문서의 단계 |
|---|---|
| `PHASE_PLAN.md` (정본) | S0~S7 / 명세 Phase 1~5 |
| `~/.claude/plans/a-issue-atlas-cozy-lagoon.md` | 그 문서 자체의 Phase -1/0/1/2 — **전부 S4 내부** |
| ~~`NUCLENS_SPEC.md`~~ | **존재하지 않음** (git 전 이력 0건). PHASE_PLAN 상단 참조 |

Atlas 계획서의 "Phase 1(팔레트)"은 명세 Phase 1(랭킹 신뢰도)이 **아니다.**

**S4 는 선행(S2·S3)을 건너뛰고 착수됐다.** 지금까지 충돌은 없지만 대가가 하나
확정돼 있다 — S3(문구 786건 외부화)를 나중에 하면 S4 에서 하드코딩한 화면 문구를
다시 들어내야 한다.

---

## 2. S4 에서 끝난 것

| 항목 | 커밋 |
|---|---|
| 딥 포레스트 팔레트 (토큰·용도제한·12.5px·다크·og) | `d6621aa` |
| 레이아웃·타이포그래피 | `26c1d3e` `7ee41d7` `76b3382` |
| 우측 상시 근거 패널 | `4063caa` (다른 세션) |
| 발간물 정리 | `32528c4` |
| 배포 자동화 + 게이트 분리 | `cb5b7bf` `9e2efaa` |

### 레이아웃에서 실제로 바뀐 것

- 이슈 목록이 카드 → **표 4열**(`56px 124px 1fr auto`). 상자·라운드를 걷고 행 사이 선 하나
- `--r-1/2/3` = `0`. 시안 197줄에 `border-radius` 는 장식 원 1건뿐이다
- h1 `clamp(32px, 4vw, 58px)` / `-.05em` / 800. 시안은 82px 이지만 그건 짧은 문장 기준이고
  여기 h1 은 `daily_lead` 종합 문장(최대 70자)이라 82px 면 다섯 줄이 된다
- 히어로가 어두운 박스 → 캔버스 위 대형 타이포. `status-strip` 이 헤더 위 모노 바로
- 오버라인·번호는 `--ff-mono`(시스템 스택). **IBM Plex Mono 는 추가하지 않는다.**
  12.5px 하한도 낮추지 않았다 — 모노·자간·색으로 구분

### 발간물

29 → 18건. `event` 5건(Joshikai·여름학교·프로젝트 회의·워크숍) + `nonpower` 6건
(IAEA 발간물의 상당수가 FAO 공동 프로그램 — Plant Breeding·Insect Pest·Soils·
Animal Production·Food Safety 뉴스레터).

기관명은 정식명칭+약자로 통일(에경연 → 에너지경제연구원(KEEI) 등). 빌드 시점
매핑이라 이미 수집된 항목도 교정된다.

---

## 3. 남은 것 — 우선순위

### A. 추적률 0.125 — 유일한 진짜 병목

`deploy-web.yml` 첫 실행이 웹 테스트 하나로 죽으면서 드러났다:

```
test_latest_briefing_tracking_rate_meets_target
AssertionError: 0.125 not greater than or equal to 0.2
```

**코드 결함이 아니다.** 0.20 은 한때 실제로 달성했던 수치고(0.2857), 0.125 는
Atlas 계획서 §3-C 가 "현행 12.5% 대비 개선"을 목표로 적어둔 그 숫자다.

임계값은 내리지 않았다 — 내리면 신호가 사라진다. 대신 **게이트를 켜는 자리**를 나눴다:

| 어디서 | 추적률 게이트 |
|---|---|
| `crawl.yml` · 로컬 | **켠다** (데이터 품질을 보는 자리) |
| `deploy-web.yml` | **끈다** (`NUCLENS_SKIP_DATA_GATES=1`) |

건너뛴 것은 `skipped=1` 로 출력에 남아 조용히 사라지지 않는다.

**다음 세션이 할 일: 임계값 손대지 말고 추적률을 올려라.**

#### A-1. [착수됨 2026-08-03 18시] 하루치 0.125 는 지표가 아니라 잡음이었다

위 진단("0.125 = 병합기 미완")은 **절반만 맞았다.** 17일 전부를 재보니:

```
08-03 0.125 F   07-30 0.333 P   07-25 0.250 P   07-20 0.333 P
08-02 0.250 P   07-29 0.222 P   07-24 0.000 F   07-19 0.143 F
08-01 0.286 P   07-28 0.125 F   07-23 0.000 F   07-18 0.000 F
07-31 0.000 F   07-27 0.000 F   07-22 0.000 F
                07-26 0.000 F   07-21 0.000 F
```

분모가 최신 브리핑 이슈 8개뿐이라 1건에 0.125 가 움직인다. ≥0.20 은 **17일 중
10일(59%)에서 실패**한다 — 병합기 품질과 무관하게. 이건 §5 가 적어둔 교훈("데이터
지표를 배포 게이트로 쓰면 안 된다")이 `crawl.yml` 쪽에 그대로 남아 있던 것이다.
자리만 옮겼지 지표 자체가 신호가 아니었다.

조치 두 가지. **임계값 0.20 은 건드리지 않았다.**

1. **게이트를 7회차 누적으로 잰다.** `meta.tracking_window_rate`
   (`build_data.TRACKING_WINDOW_BRIEFINGS = 7`). 분모 55~60. 하루치
   `latest_briefing_tracking_rate` 는 관측용으로 남는다. 실측 7일 0.193 / 14일 0.120
   — 여전히 빨갛지만 이제 "뉴스가 한산했다"가 아니라 "병합기가 목표 미달"을 뜻한다.
2. **병합 검수 밴드 하한 0.88 → 0.84.** 사람 검토 큐 544건이 방치되는 동안 LLM 이
   본 것은 14쌍뿐이었고, 그 아래에 진짜 후속 보도가 갇혀 있었다:

   ```
   0.8610  다뉴브강 수위 최저치, 헝가리·루마니아 원전 가동 중단 (07-31)
         ↔ 헝가리 총리, 팍스 원전 일요일 가동 중단 발표 (08-02)
   0.8513  헝가리 총리, 팍스 원전 일요일 가동 중단 발표 (08-02)
         ↔ 그리스 산불, 가뭄으로 헝가리 원자력 발전소 가동 중단 (08-03)
   ```

   146쌍이 새로 판정 대상이 된다. `MAX_NEW_PAIRS_PER_RUN = 40` 으로 4회차에 나눠
   묻고, 미룬 몫은 `llm_review.deferred` 에 남는다. 0.82 까지는 안 내렸다 — 그
   구간은 "[시론] 호남 반도체…" 대 전기본 기사처럼 분야만 같은 쌍이 대부분이다.

#### A-2. [해소됨 2026-08-04 05:54] 추적률 0.2857 — 목표 통과

첫 실호출이 실제로 일어났고 효과가 확인됐다:

```
llm_review: asked=40 calls=2 approved=15 rejected=39 failed=0 deferred=108
```

| 지표 | 8/3 18:40 | 8/4 05:54 |
|---|---|---|
| **추적률 7일 누적** | 0.193 | **0.2857** ✅ |
| 최신 브리핑 하루치 | 0.125 | 0.2857 |
| `issue_match_methods.llm_approved` | 4 | 10 |
| 이슈 총계 | 110 | 104 (병합으로 감소) |
| Atlas `related_articles` | 18 (16%) | **22 (21%)** ✅ |
| Atlas `latest_change` | 6 | 10 |
| 검토 큐 | 544 | 497 |

넓힌 구간에서 40쌍 중 10건 승인 / 30건 기각(25%). 도장 찍기가 아니다.
108쌍이 미룸 상태라 앞으로 3회차쯤 더 빠진다.

**Atlas 차단 노드가 `open_question` 하나만 남았다.**

##### 곁다리 — 틀린 진단 하나를 기록해 둔다

02:49 빌드가 `calls=0 / failed=40` 으로 죽어서 "thinking 토큰이 출력 예산을
잠식한 잘림"으로 추정하고 `MAX_OUTPUT_TOKENS` 를 16384 로 올리고 분할 재시도를
넣었다(`f645a2b`). **틀렸다** — 같은 8192 코드가 05:54 빌드에서 정상 통과했다.
그 실패는 일시적(한도 또는 타임아웃)이었다.

교훈: **`except Exception` 이 사유를 지우면 정황 추론으로 메우게 되고, 정황은
틀린다.** 같은 커밋에 넣은 `stats.failure_reasons`(→ `issue_audit.json`)만이
실제로 답을 줬을 물건이다. 다음에 죽으면 추론하지 말고 그 값을 볼 것.

### B. Atlas 착수 조건 — 두 값만 보면 된다

시안의 5단계 경로 중 2칸이 안 채워진다. 정상 빌드 실측(108건):

| 노드 | 필드 | 보유율 |
|---|---|---|
| 오늘의 변화 | `latest_change` | 7 (6%) |
| 남은 질문 | `open_question` | **0 (0%)** |
| 산업 영향 | `implication` | 68 (62%) |
| 관련 보도 N건 | `article_count >= 2` | 18 (17%) |
| 공식 출처 | `official_source_count > 0` | 7 (6%) |

**5칸이 전부 채워지는 이슈 0건.** 지금 그리면 100% 빈칸 그래프다.

착수 판단은 `open_question` 과 `article_count>=2` 둘만 보면 된다. 앞의 것이 0인 한
'남은 질문' 노드를 만들 수 없고, 뒤의 것이 20% 아래인 한 '관련 보도' 노드가 대부분
숨는다. **둘 다 A 의 추적률과 같은 뿌리다** — 병합 판정기가 후보를 못 받는다.

`open_question` 판정 방법:

```bash
git show origin/main:archive/2026-08.jsonl | python -c "
import json,sys
rows=[json.loads(l) for l in sys.stdin if l.strip()]
mr=[r for r in rows if r.get('importance')=='must_read' and (r.get('archived_at') or '')>='2026-08-03T04:30']
print('수리 이후 must_read:',len(mr),'| open_question 보유:',sum(1 for r in mr if r.get('open_question')))"
```

`c82a09f`(배치 템플릿 수리) 이후 크롤이 여러 번 돌았지만 신규 기사 중 must_read 가
0건이라 게이트를 탈 기사가 없었다. must_read 가 쌓였는데도 0이면 템플릿이 아니라
게이트(`news_bot.py:829-843`)를 봐야 한다. **단 생성률을 KPI 로 삼지 말 것** —
게이트를 풀면 AI 가 전망을 지어낸다.

### C. 발간물 요약 v2 — 20 UTC 실행 후 확인

`pubs_translate.PROMPT_VERSION` 을 2로 올렸지만 **로컬에 Gemini 키가 없어 한 번도
실행되지 않았다.**

```bash
curl -s "https://nuclens-v2.pages.dev/data/publications.json?cb=$(date +%s)" \
  | python -c "import json,sys;[print(i.get('gist','-'),'|',i.get('title_kr','')[:40]) for i in json.load(sys.stdin)['items']]"
```

- gist 가 제목 재진술이 아니라 **문서 성격·범위**를 말하는지 (10건이 재진술이라 지금은 숨겨져 있다)
- `off_topic` 판정이 붙는지. 붙으면 `publication_drop_reason` 이 제목 규칙 대신 그것을 쓴다
- 남은 오탐 1건: "Mixed Contaminants and Residues in Foods". `contaminant`/`residue` 를
  규칙에 넣으면 원전 오염 문서를 잡을 위험이 커서 LLM 에 맡겼다

### D. S4 잔여 — 사용자 결정은 0건이 됐다 (2026-08-03 18시 갱신)

| 항목 | 결과 |
|---|---|
| `자주 찾는 주제` 제거 | ✅ `c9b5656` — 아카이브 주제 필터와 완전 중복이었다 |
| 주간 판세 블록 위치 | ✅ **현 위치(주간 흐름 탭) 유지로 확정** — 3안 실측 대조, 코드 변경 없음 |
| `open_question`↔`next_checkpoint` 중복 | ⏸ 값 0건이라 판단 불가 — B 가 풀려야 한다 |

판단 근거·실측 표는 [`PHASE_PLAN.md` §S4](PHASE_PLAN.md).

**대신 새 차단 요인이 하나 나왔다 — 주간 판세는 3개월간 한 번도 생성된 적이 없다.**
`weekly_reports.json` 이 전 브랜치 git 이력에 0건이고 `chore: weekly report` 커밋도
0건인데, 로컬에서 `weekly_bot.py` 경로를 끝까지 돌리면 파일은 정상 생성된다. 코드가
아니라 워크플로 실행 쪽이다. **GitHub → Actions → "Weekly report" 이력 확인이 다음
할 일** (private repo 라 저장소 밖에서는 못 본다). 상세는 `PHASE_PLAN.md` §S4.

### E. [해소됨 2026-08-03 19:50] `feat/atlas-p0-data` 전량 병합

`git cherry -v origin/main feat/atlas-p0-data` → **미병합 0건.**

| 커밋 | 처리 |
|---|---|
| `243f84e` 레이아웃 / `85c8aea` 들여쓰기 / `709d662` 설명문·서체 | 이미 main (`26c1d3e` `7ee41d7` `76b3382`) |
| `7eb952e` `curate_with_llm` 잔해 제거 | ✅ `1848679` 로 cherry-pick |
| batch 잘림 수리 (다른 세션 미커밋분) | ✅ `ae9a3e0` 로 커밋 → `7b28329` 로 cherry-pick |

막고 있던 것은 `my-projects/nuclens-upgrade` 워크트리의 **다른 세션 미커밋 작업**이었다.
4시간 36분 멈춰 있었고(마지막 파일 수정 15:03), 테스트 242개가 전부 통과하는 완성
상태여서 **사용자 승인 후 그대로 인수해 커밋**했다. 코드 내용은 손대지 않았다.

그 작업이 고친 것: 2.5-flash 의 thinking 토큰이 출력 예산을 잠식해 batch 응답이 잘리면
chunk 가 통째로 버려졌고, 버려진 기사는 fallback 큐레이션으로 큐에 들어가면서
`sent` 마킹돼 **영영 복구되지 않았다.** 이제 `GeminiTruncated` 를 따로 잡아 분할
재시도하고, 그래도 유실되면 `delivery_log.jsonl` 에 `record_type=curation_failure`
로 남긴다.

**이로써 B(`open_question` 게이트 계측)의 파일 충돌이 풀렸다** — `news_bot.py` 를
이제 자유롭게 건드릴 수 있다.

---

## 4. 배포 — 자동화 완료 (검증됨)

```
화면 코드(web/**) 를 main 에 병합  →  deploy-web.yml 즉시 배포 (1~2분)
데이터 갱신                        →  crawl.yml 짝수 UTC시 배포
```

`deploy-web.yml` 은 수집·LLM 호출이 없고 상태를 커밋하지 않는다. embeddings 캐시는
**복원만** 한다(주인은 `crawl.yml`). 배포 전에 `node --check app.js` + 웹 테스트 175개
(데이터 지표 게이트는 `NUCLENS_SKIP_DATA_GATES=1` 로 제외 — §3-A).

실측(2026-08-03): `9e2efaa` 푸시 → 17:56:12 빌드 배포, 콘솔 오류 0,
표 4열 · radius 0 · h1 57.6px 라이브 렌더 확인.

**수동 wrangler 배포를 쓰지 말 것.** 화면만 올리는 게 아니라 **데이터까지 덮는다** —
`embeddings.json` 은 git 이 아니라 Actions 캐시에 살아서, 로컬 빌드는 그것 없이
클러스터링한다. 실제로 이날 지표가 332→0, 이슈 108→119 로 무너졌다가 다음 CI
배포에서 복구됐다. 미리보기는 로컬 서버로:

```bash
python -m http.server 8790 --bind 127.0.0.1 --directory <worktree>/web/public
```

---

## 5. 이번에 데인 것

- **작업 문서의 "Phase N"을 상위 명세의 Phase N 으로 착각했다.** 이 문서 첫 판이
  Atlas 계획서의 팔레트 단계를 "Phase 1 완료"로 적어, 명세 Phase 1(랭킹 신뢰도)이
  끝난 것처럼 읽히게 만들었다. **인계 문서는 그 작업이 어느 상위 슬롯에 들어가는지
  부터 적을 것** — Atlas 계획서 §2 에 "작업 슬롯은 PHASE_PLAN 의 S4"라고 이미 적혀
  있었는데 놓쳤다.
- **계획서가 "이미 배포돼 있다"를 기능 단위로 판정하면 비주얼 개편이 통째로 빠진다.**
  Atlas 계획서 §1 표가 그랬고, 그래서 팔레트만 바뀐 채 "색깔만 바꿨네" 소리를 들었다.
  시안 대조는 기능 목록이 아니라 **실측**(폰트 크기·radius 개수·그리드 열)으로.
- **`display: contents` 로 4열 표를 만들면 제목과 요약이 벌어진다.** 자식들이 서로 다른
  그리드 행으로 갈라져 옆 열(근거, 98px) 높이가 그 사이로 배분된다(실측 42px).
  `align-self: start` 로 안 막힌다 — 행의 자동 크기는 여전히 그 항목을 센다.
  열은 CSS 로 흩지 말고 **마크업에서 형제로** 낼 것.
- **데이터 지표를 배포 게이트로 쓰면 안 된다.** 추적률은 "오늘 뉴스가 어떻게 묶였나"의
  결과지 화면 코드의 성질이 아니다. 뉴스가 한산한 날 CSS 오타 수정도 배포가 막힌다.
  임계값을 낮추지 말고 **게이트를 켜는 자리**를 나눌 것.
- **규칙과 LLM 판정을 섞을 때 `off_topic: False` 를 명시적으로 저장해야 한다.** 키가
  없으면 폴백 규칙이 돌아서 LLM 이 "관련 있음"으로 본 것을 다시 지운다.
  `is True` / `is False` / 부재 3분기.
- **"제목에서 읽어낼 수 있는 범위만" 프롬프트는 제목 재진술을 낳는다.** 지어내기
  가드레일이 과하면 모델은 안전하게 원문을 되풀이한다. 역할을 갈라 주고
  (제목=무엇 / gist=성격·범위) 나쁨·좋음 예시를 박아야 한다.
- **Windows 로컬 서버**: 백그라운드로 띄우면 `cd` 가 안 먹으니 `--directory` 를 쓸 것.
  Git Bash `pkill` 이 Windows python 을 못 죽여 **여러 프로세스가 같은 포트에 LISTENING**
  되고 오래된 것이 응답한다(수정이 반영 안 되는 것처럼 보임).
  → PowerShell `Get-NetTCPConnection -LocalPort N | Stop-Process`
