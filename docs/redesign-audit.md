# 에디토리얼 리디자인 착수 감사 (2026-08-04)

> 계획: `~/.claude/plans/nuclens-ux-ui-pure-kay.md` (rev.3 승인본).
> 이 문서는 착수 시점의 실측 기준선과, 계획의 가변 표현을 코드 현실로 확정한 기록이다.
> 리디자인 커밋 열(C1~C13)의 C1 산출물.

## 0. 기준선

- 브랜치: `feat/editorial-redesign`, 분기점 **origin/main = 660495d** (2026-08-04).
- 파일 실측: `index.html` 356줄 · `app.js` 1,947줄 · `style.css` 1,702줄 ·
  `test_prototype.py` 2,561줄 · `build_data.py` 3,058줄.
- 테스트: **웹 197개 전부 통과**(skip 1, 로컬 빌드 데이터 기준). 봇 테스트는 이번 작업이 건드리지 않는다.
- 로컬 빌드: 아카이브 740건 → 표시 249건 → 이슈 카드 135개 / 상세 페이지 125개.
  로컬엔 `embeddings.json`(Actions 캐시 전용)이 없어 이슈 수가 CI 산출과 다르다 — 정상.
- Atlas 계기판: latest_change 4% / open_question 1% / implication 62% / related 8% / official 6%.

## 1. 계획 수립(74f929a) 이후 착수 전에 바뀐 것 — C4의 전제 수정

감사·계획 사이에 다른 세션들이 같은 화면을 손봤다. **아래 결정은 전부 유지하고 그 위에 쌓는다.**

| 커밋 | 결정 | 리디자인에 미치는 영향 |
|---|---|---|
| `3ff0907` | **히어로 h1 축소 58→30px** (`clamp(23px,2vw,30px)`, 750, 1.32, -.028em). 실측 17일 전부 synthesis 0건 — 히어로가 선두 카드 제목을 58px로 되풀이하고 있었다 | `--t-hero` 토큰은 **현행 30px 계열을 박제**한다. 대형 헤드라인 복원 금지 — "선두 카드가 첫 화면의 주인공"이 확정된 방향 |
| `b75742f` | 모바일 카드 2번째 줄 = **implication 승격**(없으면 summary 폴백) + AI 배지 전 카드 고지 + 크롬 정리 | C4의 "리드 정보량 확대"와 같은 방향. 카드 본문 구조는 이미 개선됨 — C4는 리드·front 층과 괘선·간격에 집중 |
| `7caeb50` | 시사점(implication)·왜 중요(why_important)를 **별도 두 필드로 유지**, 보고서 검토 추천 라벨 | 리드 카드 블록 구성에 반영됨 — 임의 병합 금지 (`test_two_axes_survive_as_two_fields`) |
| `cac0bb9`·`35d213b` | 근거 레일 배지 대비 수정, rail 라임·민트 공존은 **결정**(통일 금지) | 레일 색 건드리지 않는다 |
| index.html | `.status-strip.ok` — **정상인 날 상태 스트립은 접힌다**. 히어로 aria-live 축소, 아이콘 버튼 aria-label | C4 마스트헤드 계획 수정: "상태 스트립 별도 유지"의 의미는 "정상 시 숨김 + 이상 시 노출" 현행 그대로 |

신규 테스트 잠금 16개 추가됨(카드 리드=해석, 히어로-리드 중복 금지, 히어로 헤드라인 클릭=이슈 열기,
목록 액션에서 공유 제거·원문 유지, 두 축 분리, 배지 각괄호 숨김 등) — C4 작업 시 전부 보존.

## 2. 현행 실측 (스타일)

- **폰트 크기 분포**: 12.5px×88 · 13.5×16 · 18×10 · 15×8 · 13×6 · 16×5 · 24/22/21/17×3 · 20/14×2 · 30/19/15.5×1.
  clamp 4곳: 히어로 h1 (23~30 / 모바일 21~26 / 랜드스케이프 20~24), 리드 h3 (25~33).
- **z-index 서열**: 1(로컬)·2(로컬)·30(pop)·90(topbar)·100(tabs)·119(scrim)·120(sheet)·150(toast)·1000(skip).
- **브레이크포인트**: 720(리드 전용 고아) / 1200↑(사이드바 온) / 1100↓ / 820↓ / 767↓(대개편) / 767+landscape / 390↓ / reduced-motion.
- **토큰 현황**: 색 `--c-*`(16 잠금 + 파생), `--ff-*`, `--sh-1..3`, `--r-1..3`(=0), `--fo-ring`, `--wrap`.
  **간격·타입·모션·z 토큰은 없음** — 전부 리터럴. C2가 추가한다.
- localStorage: `nuclens-saved-issues`, `nuclens-theme` 2종.

## 3. 데이터 현실 (리디자인이 그리는 범위)

- 구조화 엔티티 없음 — 국가(ISO)·publisher·주제뿐. 원전·기업은 자유 태그 속 비정형 → C5 레지스트리로 해결.
- `_FACILITY_NAMES`(build_data.py:102) 25개 중 **오탈자 3건**: `테믈린`(→테멜린), `세르나보다`(→체르나보다), `보글틀`(→보글).
  레지스트리에서 정식 명칭으로 등재하고 오탈자는 별칭으로 보존(기존 병합 veto 동작 불변).
- `event_date` 사실상 빈 필드 → 화면의 모든 시간은 보도·포착일. "사건 발생일" 표기 금지.
- 발간물 표지 이미지 0 → C9는 CSS 타이포그래피 표지.
- `related_articles`에 `canonical_tags` 없음 → 엔티티 매칭은 `build_issue_catalog` 안 raw 멤버 대상.

## 4. 가변 표현 확정값 (계획 rev.3 "구현 판단 원칙" 이행)

| 계획 표현 | 확정 |
|---|---|
| front 강조 "상위 2~3건" | **정확히 2건** (리드 제외 후 `#issueList` 상위 2, 데스크톱=모바일 동일) |
| 레지스트리 "50~60건" | 실데이터 스캔 후 커버리지 기준 등재, 상한 60 — 실제 수는 C5 커밋에 기록 |
| `--t-hero` | `clamp(23px, 2vw, 30px)` — 3ff0907 축소 결정 박제 (58px 아님) |
| 발간물 신규 표시 | 기존 `is_new`(14일) + 텍스트 `최근 발간` 병기, 방문 기준 전환은 C11 이후 선택 |
| 엔티티 매칭 근거 저장 | C5 착수 시 `issue_audit.json` 소비처 확인 후 결정 — 기본은 최소 레코드 additive, 불가 시 빌드 로그만 |
| 흐름 탭 date 파라미터 검증 | trend는 `date`를 소비하지 않음 → URL 픽스처는 오늘 뷰로 검증 |
| C4 폴드 기준 실측 (1440×900) | ①리드 제목+무슨 일 블록 615px ✓ ②다음 이슈 제목 1037px ✗ ③히어로 243px=27% ✓ ④상단 메타 1줄 ✓. **②는 계획의 충돌 규칙(리드 정보량 우선) 적용으로 면제** — 리드 블록 리듬을 한 단 조인 뒤(1075→1037)에도 5블록 리드가 폴드와 양립 불가. 판단 정보를 자르는 것보다 낫다 |
| C4 마스트헤드 지표 블록 | **세우지 않음** — 헤더 상태 칩(`이슈 N`)·ok-접힘 상태 스트립이 이미 그 역할(b75742f). 달라진 건수는 기존 히어로 버튼에 얹음("달라진 이슈 N건 보기") |
| front 발동 조건 | 최신 브리핑 + 지역·주제 필터 전체 + 중요도순 + 카드 보기 — 하나라도 어긋나면 해제 |

## 5. 병합·롤백

- 6병합(M1 기반 / M2 오늘 1면 / M3 엔티티·탐색 / M4 검색·흐름 / M5 발간물 / M6 저장·마감).
  각 병합은 독립 revert 가능 — 병합 커밋 revert 시 이후 병합과의 충돌은 web/** 안에서만 발생.
- 배포는 main 병합 → `deploy-web.yml`(1~2분)만. **수동 wrangler 금지**(embeddings 캐시 부재로 데이터 파괴).
- 병합 기록(SHA·deploy 실행·시각)은 C13 핸드오프에 적는다.
