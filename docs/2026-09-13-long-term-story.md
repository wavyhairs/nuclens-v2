# 장기 스토리 — 창을 재고, 판정을 검증하고, thread 를 세운다 (E·F)

> 2026-09-13. `docs/2026-09-12-event-identity.md`(D 정본) 다음 단계.
> **이 문서가 E·F 의 정본이다.** D 의 기록은 덮지 않는다.
>
> 진행 중 문서다. 각 단계가 끝날 때마다 이 파일과 커밋이 함께 움직인다 —
> 작업이 중단돼도 여기부터 이어받을 수 있어야 한다.

## 0. 시작 전에 계획을 코드에 대조했다

계획서는 `E1 → E2 → E3 → F` 였다. 실제 코드에 맞춰 보니 **E1 이 맨 앞에 설 수
없었다.** 뒤집힌 가정이 여덟 개다.

| # | 계획의 가정 | 실제 |
|---|---|---|
| B1 | `tools/recorded_replay.py` 를 창 재생에 재사용한다 | 그 모듈은 **Gemini HTTP 요청열** 재현기다(callsite: curation·dedup·issue_review·keei_match). 이슈 묶음을 다시 만들지 않는다 |
| B2 | 창은 상수 하나 | `ISSUE_WINDOW_DAYS` 는 **게이트 다섯**에 걸린다 — 기사 부착 2곳·후보 검색·canary 감사·카탈로그 cutoff/ceiling. 비용은 cutoff/ceiling 에서 난다 |
| B3 | LLM 캐시 적중 99.8% 라 새 호출 ≈ 0 | 캐시 키가 `_pair_id(기사해시, 기사해시)`다. **창을 넓혀 생기는 쌍은 정의상 한 번도 물어본 적 없는 쌍**이라 적중률 0. 게다가 키가 없으면 `issue_review` 는 병합하지 않는다 → 키를 끄고 재면 "창을 넓혀도 안 붙는다"는 **가짜 결론**이 나온다 |
| B4 | E1 다음에 E3(임베딩 보존) | `EMBEDDING_RETENTION_DAYS = 35` 이고 벡터가 없으면 `in_review_band` 가 False 라 회색지대에 **들어가지도 못한다**. 35·42일 arm 은 보존 정책을 먼저 고치지 않으면 측정 자체가 불가능 |
| B5 | 라이브 임베딩이 정상이니 그대로 잰다 | 맞다. 다만 **이 저장소 로컬에는 없다** — `embeddings.json` 이 8/15자(399 KB)고 임베딩은 git 이 아니라 Actions cache 에 산다 |
| B6 | 영속 `story_id` 를 새로 만든다 | `story_id` 는 **이미 있다**(`story_identity.py` · 기사 중복묶음의 신원). 계획이 `story_contract` 충돌은 잡았는데 이건 놓쳤다. `event_ledger.py` 도 이미 있고 **미래 일정 원장**이다 |
| B7 | Claude judge 어휘를 새로 만든다(`same/different/uncertain`) | `tools/review_queue.py` 에 이미 있다 — `SAME_EVENT · FOLLOW_UP_NEW_ACTION · FOLLOW_UP_NO_NEW_ACTION · RELATED_DISTINCT_EVENT · UNRELATED · INSUFFICIENT`. **same_event 와 same_story 를 가르는 축이 이미 코드에 있다**(`FOLLOW_UP_NEW_ACTION → issue_review MERGE / dedup SEPARATE`) |
| B8 | 6개월·수년 전 Event 재검색을 검증한다 | archive 가 2026-07(399건)·08(8,191)·09(4,425)뿐이다. **실질 코퍼스 6주.** 관측된 최대 근거 span 이 48일인 이유다 |

### 계층 이름표 — 먼저 못 박는다

```
Article  →  briefing story  →  issue / event  →  thread
            (story_id)         (issue_id)        (thread_id)
            중복묶음 신원       D 가 못 박은 사건 신원   장기 스토리 ← 새로 만드는 것
```

| 이미 쓰는 이름 | 현재 뜻 | 장기 계층이 쓸 이름 |
|---|---|---|
| `story_id` | 기사 중복묶음 신원 (`story_identity.py` v2) | `thread_id` |
| `event_ledger.py` | 달력 창 밖 **미래 일정** 원장 | `thread_relations` |
| `story_contract` / `STORY_CONTRACT_VERSION` | 기사 story 계약 v2 | `story_scope` |
| `issue_id` | Event 신원 | 그대로 — 공개 계약을 깨지 않는다 |

### 재배치한 순서

```
E0   하니스와 임베딩         ← 새로 생긴 단계 (B1·B5)
E3a  보존 정책               ← E1 보다 앞으로 (B4)
E1   21/28/35/42 재측정
E2   독립 판정 (기존 어휘로)
E3b  장기 retrieval
F    thread 계층 (shadow)
```

---

## E0. 하네스와 임베딩 — 완료

### 무엇을 만들었나

**`web/build_data.py` — 창에 구멍 하나.** 상수를 고치지 않는다. 환경변수
`NUCLENS_ISSUE_WINDOW_DAYS` 가 없으면 production 은 지금과 한 글자도 다르지 않게
돈다(기본값 21). 2,484건의 검사가 이 값에 묶여 있어 상수를 옮기는 변경은 회귀를
대량 생산한다.

**격리는 전수여야 했다.** 첫 재생을 돌리고 `git status` 를 보니 손대지 않은 파일이
바뀌어 있었다.

```
issue_insights.json    463줄
keei_llm_matches.json   72줄
```

빌드는 `OUTPUT_DIR` 밖에도 쓴다. 원장과 판정 캐시만 격리하고 이 둘을 몰랐다.
`ISSUE_REVIEW_CACHE_FILE` · `ISSUE_INSIGHT_CACHE_FILE` · `KEEI_MATCH_CACHE_FILE` ·
`ISSUE_LEDGER_FILE` 넷을 열고 재생기가 arm 사본을 넘긴다.

**`tools/window_replay.py` — 창 재생기.** 비교 단위는 **이슈 id 가 아니라 기사
쌍**이다. 창이 바뀌면 이슈 id 는 통째로 갈릴 수 있지만 "이 두 기사가 한 묶음에
있었나"는 창을 건너 비교된다. 원장과 판정 캐시는 arm 마다 **같은 기준 사본**에서
출발한다.

### 임베딩 — 로컬은 죽어 있었다

`embeddings.json` 이 **8월 15일자**(399 KB)였고 `local_embeddings.json` 은 없었다.
임베딩은 git 이 아니라 Actions cache 에 산다. 그대로 재면 remote embedding 경로가
통째로 죽은 시스템을 측정한다.

```
embedding_pipeline.py --window-days 70 --max-new 1200
    selected 747 · current 747 · generated 477 · failed 0 · coverage 1.0
```

첫 시도는 **한 줄 때문에 죽었다** — 생성 실패 한 건의 한국어 로그가 Windows
cp1252 stdout 에서 `UnicodeEncodeError` 를 내며 스크립트 전체를 중단시켰다
(311건에서 멈춤). Actions 는 UTF-8 이라 거기서는 안 난다. 로컬 재생은
`PYTHONIOENCODING=utf-8` 이 필요하다.

> ⚠️ 이 재생은 production 보다 **유리한 조건**이다. 운영에서는 21일 백필 + 35일
> 보존이라 40일 전 기사의 벡터가 이미 없다. 그래서 E3a 가 E1 앞에 온다.

---

## E3a. 보존 정책 — 완료

`EMBEDDING_RETENTION_DAYS = 35  # 21일 이슈 창 + 재실행 여유` 는 결합이 **주석에만**
있었다. 창을 옮기는 순간 조용히 깨진다.

```python
ISSUE_WINDOW_DAYS = _positive_int_env("NUCLENS_ISSUE_WINDOW_DAYS", 21)
EMBEDDING_RETENTION_MARGIN_DAYS = 14
EMBEDDING_RETENTION_DAYS = _positive_int_env(
    "EMBEDDING_RETENTION_DAYS", ISSUE_WINDOW_DAYS + EMBEDDING_RETENTION_MARGIN_DAYS)
```

기본값 35 로 **지금과 같다.** 백필 창도 이슈 창을 따라간다 — 워크플로에 박힌
`--window-days 21` 을 뺐다. 둘이 어긋나면 창 안에 있는데 벡터가 없는 기사가 생기고,
그 기사는 `in_review_band` 에서 `embedding_similarity=None` 으로 탈락해 회색지대에
들어가지도 못한다.

---

## E3b. 장기 retrieval — 완료

### 호기 이름이 한국어에서만 통했다

호기를 읽는 자리는 `build_data._UNIT_RE` 하나인데 `호기` 를 요구하고
`_FACILITY_NAMES` 25개가 전부 한글이다. **"Kori Unit 2" 는 호기 검사에 걸리지조차
않았다.** "한빛 3호기와 4호기는 어떤 기사를 경유해도 같은 사건이 아니다"라는 거부권이
영문 기사에는 서지 않고 있었다.

`entity_registry.json` 도 못 메운다 — plant 26건이 발전소 단위까지고 호기가 없다.
`asset_alias.py` 가 레지스트리의 별칭·`name_en` 을 그대로 쓰고 **호기 번호를 붙이는
규칙만** 더한다.

```
고리 2호기 · 고리2호기 · Kori Unit 2 · Kori-2 · Kori 2  →  {"kori-2"}
고리 3·4호기 · Hanbit 3 and 4                            →  두 토큰
신고리 2호기                                              →  shin-kori-2 하나만
```

### 원장이 이미 장기 원본이었다

`issue_ledger.json` 은 지우지 않는다(798건 · 792 KB · git 추적). 들고 있는 것이
제목·요약·해시뿐이라 검색할 거리가 없었을 뿐이다. `catalog_rows` 에 엔티티·호기·
국가·지문·근거일자를 더하고 **덮지 않고 쌓는다**.

이미 쌓인 798건은 `tools/backfill_ledger_retrieval.py` 가 아카이브에서 소급해 채운다.
빌드를 한 번 더 돌려 채우지 않은 이유: 그 방법은 지금 창의 60일 카탈로그만 건드려
7월 사건이 영영 빈 채로 남는다.

```
798건 채움 · 엔티티 291건(36%) · 호기 40건 · 근거 기사 없음 54건
지문 0건 — 아카이브 레코드에 story_fingerprint 가 없다(빌드 단계 산물).
           앞으로 쌓이는 사건은 catalog_rows 가 채운다.
```

### 색인은 파생물이다

```
사건 798건 · 색인 0.03초 · 491 KB · 질의 ~1 ms
```

외부 Vector DB 를 쓰지 않는다. 그리고 **임베딩에 기댈 수도 없다** — 벡터는
`EMBEDDING_RETENTION_DAYS`(창 + 14일) 만큼만 살고, 로컬 폴백 벡터는 빌드마다 다른
IDF 공간이다. 즉 의미 검색은 원리적으로 최근 구간만 덮는다. **장거리는 구조화
신호와 어휘가 진다.**

실측 — 창(21일) 밖에서 같은 호기 사건이 상위에 선다:

```
[2026-07-17] 한빛 1·2호기 계속운전, 원안위 검토 작업 가속화
   14.71  gap=20일  고리 3·4호기 올해, 한빛 1·2호기 내년 계속운전 심사 상정 예정
   14.69  gap=57일  원안위, 고리 3·4호기 및 한빛 1·2호기 사고관리계획 심의 보류
   13.07  gap=50일  한빛 1·2호기 열교환기 보수작업 용접절차서 오적용 6건 확인  ← 다른 이야기
```

마지막 줄이 이 단계의 한계를 그대로 보여준다. 같은 호기지만 계속운전과 정비는
다른 이야기다. **후보 검색은 recall 이고 그것을 가르는 것은 판정의 일이다.**

---

## E1. 창 재측정 — 완료

`web/_windows/report.json`. as-of `2026-09-12T23:00:00+09:00` 고정 · 창마다 3회 재생
(1회차는 판정 캐시를 채우는 회차라 멱등성은 **데워진 뒤 두 회차** 사이에서 잰다) ·
LLM 켬 · 원장과 판정 캐시는 arm 마다 같은 기준 사본에서 출발.

| 창 | 이슈 | 연결 | 단발률 | span중앙 | >7일 | >21일 | 최대 | 근거행 | 후보풀 | 신규판정 | 상속률 | 멱등 | 초 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| **21일** | 519 | 1,439 | 0.794 | 5 | 113 | 40 | 54 | 1,958 | 26,501 | 7 | 0.9942 | 0.9981 | 330 |
| **28일** | 515 | 1,495 | 0.792 | 6 | 125 | 59 | 54 | 2,010 | 30,640 | 336 | 0.9961 | **1.000** | 313 |
| **35일** | 511 | 1,493 | 0.787 | 6 | 128 | 64 | 54 | 2,004 | 32,916 | 505 | 0.9961 | 1.000 | 325 |
| **42일** | 509 | 1,498 | 0.788 | 7.5 | 130 | 67 | 54 | 2,007 | 33,738 | 561 | 0.9961 | 1.000 | 328 |

`최대 54일` 이 네 창에서 모두 같다. **창이 그 사건을 자르고 있던 것이 아니다** —
그 묶음은 21일에서도 이미 54일치를 들고 있었다(D 문서가 "21일을 넘는 사건 묶음이
이미 55건 있다"고 적은 것과 같은 사실이다).

### delta — 더 긴 창 때문에 새로 생긴 것

| 구간 | 쌍 +| 쌍 − | 순증 | 이슈 | id 유지 |
|---|---|---|---|---|---|
| 21 → 28 | +3,023 | −1,330 | **+1,693** | −4 | 0.973 |
| 28 → 35 | +1,152 | −1,987 | **−835** | −4 | 0.977 |
| 35 → 42 | +1,035 | −710 | +325 | −2 | 0.980 |

**28 → 35 는 얻는 것보다 잃는 것이 많다.** 그 구간에서 새로 붙는 쌍(1,152)보다
떨어지는 쌍(1,987)이 더 많다. 이것은 재현율이 아니라 **churn** 이다 — 후보 풀이
커지면서 묶음 경계가 다시 그려지는 것이지 후속 근거를 더 찾은 것이 아니다.

### 매칭 경로 — 늘어난 것이 어디서 왔나

| 창 | tags | title_tags | title | llm_approved | embedding |
|---|---|---|---|---|---|
| 21일 | 806 | 267 | 189 | 102 | 66 |
| 28일 | 842 | 274 | 200 | 105 | 65 |
| 35일 | 837 | 275 | 196 | 108 | 68 |
| 42일 | 839 | 274 | 196 | 113 | 68 |

21→28 의 +56 중 **+36 이 `tags`**, +11 이 `title` 이다. LLM 판정 경로는 +3 뿐이다.
증거의 질로 보면 늘어난 것은 **약한 쪽**이다 — 그래서 E2 가 필요하다.

### 비용

신규 LLM 판정은 창을 넓힐 때 **한 번만** 든다(캐시 키가 기사 해시 쌍이라 창과
무관하게 재사용된다).

```
21일    7건   ← 현재 운영 창. 캐시가 이미 덮고 있다
28일  336건   (+329)
35일  505건   (+169)
42일  561건   (+56)
```

계획서의 "캐시 적중 99.8% 라 새 호출 거의 0" 은 **현재 창에 대해서만** 맞았다.
실행 시간은 네 창이 313~330초로 사실상 같다 — 후보 풀이 27%
늘어도 벽시계는 움직이지 않는다. 비용의 실체는 시간이 아니라 판정 수다.

### 신원 — 창을 넓히면 오히려 안정된다

예상과 반대였다. 21일에서 멱등성이 0.9981(데워진 두 회차 사이에서도 1건이 이동)인데
28일 이상에서는 **1.000** 이다. 상속률도 0.9942 → 0.9961.

원인은 `should_reask` 다. 21일에서는 묶음이 작아 제목 어휘 겹침이 판정 이후에도
계속 움직이고, 그때마다 거부 판정을 다시 묻는다(1회차 재질의 167건). 창이 넓으면
묶음이 먼저 안정되어 재질의가 줄고, 그래서 회차 간 이동도 사라진다.

> **기록해 둘 것:** 라이브 LLM 을 켜면 이 계는 **비트 단위로 멱등하지 않다.**
> `should_reask` 가 근거 변화에 반응하도록 설계돼 있기 때문이다(그게 옳다).
> 멱등성은 "캐시가 데워진 뒤"에만 의미가 있다.

### E1 만으로 내린 잠정 판단

우선순위대로 — precision 은 E2 가 답할 것이므로 여기서는 나머지만 본다.

1. **신원 안정성**: 28 이상이 21보다 낫다 (0.9981 → 1.000)
2. **실제 후속 근거**: 21→28 에서 span>21 이 40→59 (+19), 근거행 +52. 28 이후로는
   +5, −6 으로 멈춘다
3. **비용**: 28 은 +329 판정, 35 는 거기서 +169 를 더 쓰고 **연결을 2개 잃는다**
4. **복잡도**: 동일

**28일이 후보.** 35·42 는 어느 축으로도 28 보다 낫지 않다 — 동률이면 짧은 창이라는
규칙에 따라 탈락. 21 과 28 사이는 E2 의 오병합 판정이 가른다.


## E2. 독립 판정 — 완료

blind 470쌍을 8조각으로 나눠 **별도 컨텍스트 8개**에 맡겼다(같은 세션에서 수백 쌍을
연달아 보면 앞의 판단이 뒤의 기준이 된다). 1차에서 여덟 모두 자료를 다 읽고
**판정을 쓰기 직전에** 계정 세션 한도에 걸려 죽었다 — 두 조각만 남았다. 한도가
풀린 뒤 여섯을 다시 돌려 **364건**을 받았다.

> 1차에 살아남은 117건으로 "창이 넓을수록 정밀도가 단조 감소한다(0.36→0.26→0.21)"
> 고 적었는데, **전수에서 뒤집혔다.** 표본 30~45건짜리의 잡음이었다. 아래가 맞다.

### Gemini 와의 대조는 성립하지 않았다 — 그리고 그것이 발견이다

364건 중 production 판정이 있는 것이 **34건뿐**이다(330건 `gemini_absent`).

원인은 명확하다. delta 는 **같은 이슈에 함께 놓인 기사 쌍**인데 `issue_review` 는
코사인 밴드 `[0.84, 0.92]` 안의 쌍만 묻는다. **창을 넓혀 생긴 연결의 91%는 LLM 을
거치지 않는다** — E1 의 매칭 경로 표가 말한 그대로다(+56 중 tags +36, llm +3).

> 계획의 "Gemini 판정과 join 한다" 는 **모집단을 잘못 잡았다.** 창이 만드는 연결과
> LLM 이 보는 쌍은 거의 겹치지 않는 두 집합이다.

34건에서의 일치율은 0.735(같은 사건 12 · 다른 사건 13 · 불일치 9). 사람 라벨과의
3축 대조에서는 Claude 7/12, Gemini 6/12 로 **두 모델이 비슷하게 맞고 비슷하게
틀린다** — 일치율을 정확도로 읽으면 안 되는 이유다.

### 실제로 잰 것 — 새 연결의 정밀도

이 쌍들은 **모두 실제로 한 이슈에 묶인 쌍**이므로, 독립 판정이 "같은 사건 계열이
아니다"라고 하면 그것이 오병합이다.

| 층 | n | 같은 사건 계열 | RELATED_DISTINCT | UNRELATED | **정밀도** | 그중 same_thread |
|---|---|---|---|---|---|---|
| 21 → 28 | 119 | 40 | 38 | 41 | **0.34** | 66 (55%) |
| 28 → 35 | 117 | 44 | 57 | 16 | **0.38** | 81 (69%) |
| 35 → 42 | 116 | 38 | 47 | 31 | **0.33** | 70 (60%) |

**창을 넓히는 어느 단계든 새로 붙는 쌍의 3분의 1만 같은 사건이다.** 넓힐수록
나빠지지도 않는다 — 처음부터 고르게 나쁘다. 가장 흔한 축은 `topic_only`(87),
`same_entity_different_issue`(79), `different_stage`(75)다.

### 그런데 마지막 칸이 이 작업 전체의 답이다

같은 사건이 아니라고 판정된 쌍의 **대부분이 같은 장기 스토리다**(55~69%).

> 창을 넓혀 잡히던 것은 **Event 재료가 아니라 Story 재료였다.** 창은 그것을
> 담을 그릇이 아니다 — 담으면 오병합이 되고, 담지 않으면 잃는다. 그래서 답은
> 창을 넓히는 것이 아니라 **계층을 하나 더 두는 것**이다.

`different_stage` 75건이 그 증거다. "심사 착수 → 심의 보류 → 결정" 은 서로 다른
사건이고 한 이야기다. Event matcher 에게 그것을 합치라고 하면 안 된다.

## E1 + E2 최종 결정 — **21일 유지**

| 근거 | 내용 |
|---|---|
| 창이 벽이 아니었다 | 최대 근거 span 이 **네 창 모두 54일**. 21일에서도 그 묶음은 이미 54일치를 들고 있었다 |
| 28→35 는 손해 | 새로 붙는 쌍 1,152 < 떨어지는 쌍 1,987. 재현율이 아니라 churn |
| 늘어난 것이 약한 쪽 | 21→28 의 +56 중 +36 이 `tags`. LLM 경로는 +3 |
| 독립 판정 | 어느 단계든 새 쌍의 정밀도 0.33~0.38 |
| 잡히던 것의 정체 | 그 쌍의 55~69%가 **같은 사건이 아니라 같은 스토리**다 |
| 비용 | 28 은 +329 신규 판정, 35 는 +169 를 더 쓰고 연결을 2개 잃는다 |

**28일이 이긴 축이 둘 있다** — 멱등성(0.9981 → 1.000)과 span>21(40 → 59건).
그 둘을 사는 값이 "새로 붙는 쌍의 3분의 2가 오병합"이다. precision 우선 규칙에서는
살 수 없다.

> **그리고 창을 넓힐 필요가 없다는 것이 F 에서 증명됐다.** 21일 창을 유지한 채
> 원장 기반 retrieval 로 **59일 떨어진 사건**을 같은 스토리로 잇는다.

## F. thread 계층 — 그림자 완료

### 만든 것

| 모듈 | 하는 일 |
|---|---|
| `thread_judge.py` | `same_thread` 판정. **`issue_review` 와 다른 계약** — 파일·키·PROMPT_VERSION 을 나눈다 |
| `thread_identity.py` | 묶고, 이름을 정하고, 오염을 막는다. 기본 단위는 기사 해시가 아니라 **사건 id** |
| `thread_ledger.py` | 관계를 덧쓰지 않고 쌓는다(attach·detach·merge·split·revoke·supersede) |
| `tools/build_threads.py` | 그림자 빌드. production UI 는 읽지 않는다 |

### 실측 (2026-09-13, 원장 798건)

```
후보 2,794쌍  →  규칙거부 1,304  ·  LLM 판정 1,482 (호출 124 · 실패 0)
승인 고리 445  →  결합 203       ·  거부: 약한고리 23 · 성장 12 · 앵커불일치 31 · 앵커없음 80
스토리 110개 · 사건 313건 (원장의 39%)
크기: 2건 71 · 3건 18 · 4건 9 · 5건 3 · 6건 2 · 7건 2 · 8건 5
수명: 중앙 9일 · 최대 59일 · 30일 이상 13건
색인 0.03초 · 491 KB · 판정 재사용 시 전체 재군집 0.6초
```

### F8 신원 안정성 — 3회 재생

```
round1→2  110→110  유지 110 (1.0000)
round2→3  110→110  유지 110 (1.0000)
구성까지 동일 110/110 · 중복 thread_id 0 · 반복 시 새 관계 0 (멱등)
```

### F10 실제 스토리

**계속운전 — 한빛 1·2호기** (5건 · 59일)
```
07-14  한빛 1·2호기, 7년 만에 트립 결함 해소…계속운전 청신호
07-17  한빛 1·2호기 계속운전, 원안위 검토 작업 가속화
08-29  정부, 12차 전기본 수립 착수 및 원전 계속운전 기간 확대 협의   ← 잡음 1건
09-09  한빛 2호기, 9월 11일 설계수명 만료로 가동 정지
09-11  한빛 2호기, 계속운전 심사 지연으로 11일 가동 중단
```
**21일 창으로는 첫 사건과 마지막 사건이 같은 이슈가 될 수 없다(59일).** 원장
기반 retrieval 이 잇는다.

**기업 프로젝트 — 홀텍 SMR-300** (6건 · 35일)
```
08-07  현대건설, 美 홀텍·엔터지와 SMR-300 도입 가능성 검토
08-28  홀텍, 아이다호 국립연구소에 SMR-300 실증 시설 구축
08-29  홀텍, SMR-300 실증 시험 착수…현대건설 인허가 연동
09-02  홀텍, INL 에 SMR-300 테스트 시설 구축
09-09  홀텍 뉴클리어, 최대 9억 달러 규모 IPO 추진
09-11  미국 홀텍 인터내셔널, IPO 로 최대 9억 달러 조달 추진
```

**정책·법제 — 미 하원 원자력 인허가 개혁** (3건 · 52일)
```
07-15  미 하원 에너지 소위원회, 6개 원자력 관련 법안 심의 예정 발표
07-16  미국 하원 소위원회, 6개 원자력 인허가 개혁 법안 승인
09-05  미 하원, 신규 원전 건설 및 인허가 개혁 법안 논의
```

**계속운전 — 월성 2·3·4호기** (2건 · 38일) — **다음 관전점이 붙은 유일한 스토리**
```
08-01  월성2호기 계속운전 및 지역재원 확보 논의 동향
09-08  경주시, 월성 2·3·4호기 계속운전 및 SMR 건설 지원 건의
관전점  2026-11-01  설계수명이 만료  (event_ledger.json · 호기로 매칭)
```

**신규 사업 — DOE 원자력 혁신 캠퍼스** (3건 · 44일)
```
07-29  미국 에너지부, 원자력 혁신 캠퍼스 유치 후보지 5개 주 선정 발표
07-30  (같은 사건 다른 매체)
09-11  웨스트버지니아주, DOE 원전 수명주기 혁신 캠퍼스 최종 후보 선정
```

### 남은 문제

* **호기 앵커가 있는 스토리가 110개 중 8개뿐이다.** 원장 소급 채움에서 호기가
  잡힌 사건이 40건뿐이라(제목에 호기가 안 적힌다) 대부분의 스토리가 엔티티·어휘로만
  묶인다. 가장 강한 신호를 거의 못 쓰고 있다.
* **정책 스토리가 8건에서 잘린다.** 12차 전기본은 그 자체로 20여 건짜리 긴
  이야기인데 앵커가 없어 둘로 쪼개진다. precision 을 앞에 둔 대가다 —
  과대병합은 화면에 나가면 되돌릴 수 없지만 과분열은 규칙 하나로 이을 수 있다.
* **지문이 비어 있다.** 아카이브 레코드에 `story_fingerprint` 가 없어 소급
  채움에서 `facts` 가 0건이다. `action`·`occurrence` 축을 판정에 못 쓰고 있다.
* F11(스토리 독립 판정)은 **도구만 만들고 돌리지 못했다** — E2 와 같은 세션 한도.

### F12 — 기존 스토리 메뉴에 연결하지 않는다

현재 "스토리" 메뉴는 `issues.json` 을 **클라이언트에서 거르는 화면**이다
(`briefing_count >= 2 || 서로 다른 날짜 근거 >= 3`). 목록의 항목이 이슈다.
thread 는 그 이슈들을 묶은 **상위 객체**라 목록의 단위 자체가 바뀐다.

앵커율 8/110 과 정책 스토리 과분열이 남아 있는 상태로 그 화면을 바꾸지 않는다.
**shadow 까지가 이번 F 의 결과다.**
