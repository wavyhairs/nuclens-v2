# 모바일 UX 감사·수리 (2026-08-03)

기준선: `origin/main = 871388c` · 실측 도구 gstack browse · 라이브 https://nuclens-v2.pages.dev

**이번 작업은 재설계가 아니라 수리다.** 모바일 IA(하단 탭 5개 · 상단 통합검색 ·
히어로 1문장 · 날짜 이동)는 P4~P5 에서 이미 자리를 잡았고, 실측해 보니 페이지
가로 스크롤 0 · 대비 위반 0 · 시맨틱/제목 위계 정상이었다. 바꿀 이유가 있는
것만 바꿨다.

## 실측으로 확인한 결함

| # | 결함 | 근거(실측) | 등급 |
|---|---|---|---|
| 1 | `.source-link` 61×15px | WCAG 2.2 SC 2.5.8 최소 24×24 미달. `#newsList` 전 항목 | 긴급 |
| 2 | `.issue-change` 가 모바일에서만 잘림 | `max-height:70px; overflow:hidden` — 잘린 뒷부분을 볼 경로가 화면에 없음 | 긴급 |
| 3 | 상태 스트립 가로 잘림 | `scrollWidth 714` vs `clientWidth 390`, `white-space:nowrap` + 스크롤 힌트 0 | 긴급 |
| 4 | 상세 모달 액션이 글자 단위 줄바꿈 | 360px 에서 버튼 폭 45px → "저/장" | 긴급 |
| 5 | 아카이브 필터가 결과를 폴드 밖으로 밀어냄 | 360×800 에서 필터 패널이 첫 화면 520px 점유, 결과 119건은 스크롤 후 | 높음 |
| 6 | 터치 타깃 44px 미달 | 헤더 3개 40×40, 날짜 ←/→ 36×36, 정렬 select·필터 36px, 브랜드 34px | 높음 |
| 7 | `.trend-intro` 가 391~767px 에서 안 접힘 | h1 '주간 흐름'이 오버라인과 떨어져 오른쪽 끝에 홀로 섬 | 높음 |
| 8 | `.section-heading` 이 모바일에서 row 유지 | '이번 주 움직인 이 / 슈' 낱말 중간 줄바꿈 | 높음 |
| 9 | 바텀시트에 ESC·스크림·포커스 복귀 없음 | `<details>` 는 이 셋을 주지 않는다. 닫는 길이 '닫기' 버튼뿐 | 높음 |
| 10 | `aria-live` 7개 중 2개가 과다 | `#issueList`(카드 8장 전체) · `.briefing-hero`(h1+칩+버튼) | 높음 |
| 11 | 본문 15.5px / 요약 15.5px | 모바일 본문 16px 기준 미달 | 보통 |
| 12 | `.issue-dialog max-height: 100vh` | 모바일 주소창을 계산에 안 넣음 | 보통 |
| 13 | 가로 모드에서 탭바가 세로의 18% | 740×360 에서 64px 고정 + body padding 68px | 보통 |
| 14 | `.issue-topic { display:none }` | 필터의 기준 축인 주제를 카드에서 지움 | 보통 |
| 15 | 검색 입력 라벨이 `sr-only`, `enterkeyhint` 없음 | 모달 y=304 라 키보드가 올라오면 입력창이 덮임 | 보통 |

## 고친 방식에서 재사용할 만한 것

- **가로 스크롤 스트립은 잘림을 숨기는 장치다.** 항목마다 `<span>` 을 주고
  구분자를 `::before` 로 그리면 좁은 화면에서 낱말이 아니라 **항목 단위**로
  접힌다. 실측 714px 1줄 → 366px 2줄(54px), 정보 손실 0.
- **`<details>` 를 바텀시트 모양으로 만들면 바텀시트의 계약도 져야 한다.**
  스크림은 `::before` 라 클릭 `target` 이 `details` 자신으로 잡힌다 —
  `event.target === drawer` 로 스크림 탭을 구분할 수 있다.
- **한 컴포넌트를 데스크톱 사이드바 / 모바일 시트로 겸용**할 때는 CSS 로
  `open` 을 흉내내지 말고 `matchMedia` 로 `open` 속성을 관리할 것. UA 가
  `details` 내용 표시를 `display` 로만 다루지 않아 CSS 오버라이드가 깨진다.
- **바텀시트가 결과를 덮으면 적용 결과를 시트 안에 다시 써야 한다.**
  `#filterSheetCount` · `#archiveSheetCount` 가 그 역할.
- **`.section-count { order: -1 }` 은 부모가 grid 로 바뀌는 순간 의미가 뒤집힌다.**
  개수가 제목 위로 올라갔다. 정렬 규칙은 그것이 사는 컨테이너로 스코프할 것.
- **12.5px 하한 테스트는 '미만'만 잡는다.** 올리는 방향은 안전하다 — 액션
  라벨(저장·공유·원문)은 13.5px 로 올렸다.

## 검증

- 웹 테스트 176개 통과(`NUCLENS_SKIP_DATA_GATES=1`), `node --check app.js` 통과
- 320/360/390/412/430/768 가로 스크롤 0, 24px 미만 터치 타깃 0
- 라이트·다크 대비 위반 0 (전 텍스트 노드 계산, AA 기준)
- 가로 모드 740×360 가로 스크롤 0, 탭바 50px
- 데스크톱 1280 아카이브 사이드바 회귀 없음(`grid 230px + 982px`, summary 숨김)
- 바텀시트 ESC 닫힘 + 포커스가 summary 로 복귀, 스크림 탭 닫힘, 배경 스크롤 잠금

## 남은 것

- `.issue-summary` 는 여전히 3줄 클램프다. 전문은 상세에서 보지만 카드에서
  '더 보기' 를 원하는지는 사용자 판단.
- 발간물 기관 필터 칩이 320px 에서 4줄(약 170px)을 쓴다. 칩 수가 늘면 스크롤
  가능한 한 줄로 바꾸는 편이 낫다.
- 검색 결과가 없을 때의 빈 상태는 있으나, **검색 실패 원인별 안내**(오타 추정
  등)는 없다.
- `role="status"` 를 시트 안에 둔 탓에 시트를 열 때 개수가 한 번 더 낭독될 수
  있다. 실기기 스크린리더(TalkBack/VoiceOver) 확인은 못 했다 — 헤드리스 한계.
