# 실행 계획 (정본) — S0~S7

> ## ⚠️ 이 문서가 유일한 정본이다 (2026-08-03 승격)
>
> 아래 본문은 `NUCLENS_SPEC.md` 를 상위 문서로 두고 쓰였지만, **그 파일은 존재하지
> 않는다.** 실측:
>
> - 저장소 `find` 0건 · **git 전 이력 `--diff-filter=A` 0건** (한 번도 커밋된 적 없음)
> - `~/.claude` · `Documents/Codex` 전역 검색 0건
>
> 대화에서만 오갔고 파일로 저장되지 않은 것으로 보인다. 그런데 이 문서를 포함해
> **4개 문서가 34회 참조**한다(`AS_IS.md` · `score_distribution.md` · `ui_strings.md`).
>
> **그래서 이 문서를 정본으로 승격한다.** 앞으로 "명세"라는 말이 나오면 그건 읽을 수
> 없는 문서다 — 아래 규칙을 따를 것:
>
> | 참조 형태 | 어떻게 다룰 것인가 |
> |---|---|
> | `명세 Phase 1~5` | **번호 매핑으로만 유효.** 각 S 슬롯의 본문이 실제 내용이다 |
> | `명세 N장 M-K` (4장 1-1, 5장 2-2, 6장, 12장) | **복원 불가.** 그 항목은 착수 전 다시 결정할 것 |
>
> 복원 불가 참조가 걸린 항목은 §S0·§S4 에 있다. 그것들은 "명세대로"가 아니라
> **다시 판단해서 여기 적고** 진행한다. 없는 문서를 근거로 쓰면 안 된다.
>
> 상위 문서를 나중에 찾으면 이 블록을 지우고 다시 링크할 것.

Phase 0 실측([`AS_IS.md`](AS_IS.md) · [`score_distribution.md`](score_distribution.md))에
맞춰 짠 실행 순서다.

**원안과 달라지는 핵심 하나**: 원안이 §5 끝에 *"별건으로 남는 것"* 으로 밀어놓은
**features 결손이 실은 S2(랭킹 신뢰도)의 선행 조건**이다. 이걸 뒤로 미루면 S2-1의 임계값을
두 번 정하게 되고, S2-4는 상투어를 낳는다.

---

## 전체 순서

상태는 **2026-08-03 실측**이다. 갱신할 때 근거(커밋 해시·검색 결과)를 같이 적을 것.

| 단계 | 내용 | 명세 번호 | 선행 | 상태 (2026-08-03) |
|---|---|---|---|---|
| **S0** | 브랜치 정리 + 명세 개정 | — | — | ⚠️ 브랜치 정리 ✅ / **명세 개정은 대상 문서 부재로 무효** |
| **S1** | features 결손 해소 | §5 "별건" → 승격 | S0 | ✅ 코드 완료 (`aa3315d`) · **관찰 2주 대기** |
| **S2** | 랭킹 신뢰도 | Phase 1 | S1 | ⚠️ 2-1 ✅ (`72ce421`) / **2-2~2-4 미착수** |
| **S3** | 문구 교체 (중립분만) | Phase 2 | S0 | ❌ 미착수 — 대상 786건 (`ui_strings.md`) |
| **S4** | 첫 화면 재구성 | Phase 3 | S2·S3 | 🟡 **진행 중** — 아래 참조 |
| **S5** | 민감도 제어 | Phase 4 | S4 | ❌ 개념 자체 없음 (`sensitivity_level` 검색 0건) |
| **S6** | 유머 문구 활성화 | Phase 2의 유머 결 | **S5** | ❌ 미착수 (`copy.json` 없음) |
| **S7** | 사건 추적 | Phase 5 | S2 | ❌ 미착수 (이슈 ID 지속성 0건) |

원안 대비 바뀐 것: ①S1 신설·승격 ②Phase 2의 유머 부분을 떼어 S6으로 분리(민감도 없이
유머를 켜면 "안전한 쪽으로 실패"가 구조적으로 불가능) ③Phase 1이 S1 뒤로.

### ⚠️ S4 는 선행을 건너뛰고 착수됐다 (2026-08-03)

S4 의 선행은 `S2·S3` 인데 둘 다 미완인 채로 시작됐다. 팔레트·레이아웃 작업이라
지금까지 충돌은 없었지만 **대가가 하나 확정돼 있다**: S3(문구 786건 외부화)를 나중에
하면 S4 에서 하드코딩한 화면 문구를 다시 들어내야 한다. 진행 내역은
[`2026-08-03-signal-desk-handoff.md`](2026-08-03-signal-desk-handoff.md).

---

## S0. 브랜치 정리 + 명세 개정 (선행 차단 해제)

> **2026-08-03 갱신**: 1·2번(브랜치 정리)은 끝났다 — `feat/editorial-layer` 스택은
> patch-id 기준으로 main 에 반영돼 있고, 그 뒤 작업도 전부 main 에 있다.
> **3번 "명세 개정 5곳"은 무효다** — 개정할 대상 문서가 존재하지 않는다(문서 상단 참조).
> 그 표의 5개 항목은 "명세를 고친다"가 아니라 **"여기서 다시 결정한다"로 바꿔 읽을 것.**
> 그중 실제로 살아 있는 것은 아래 둘뿐이고 나머지 셋은 이미 반영됐다:
>
> - `display_cap_*` 는 신설이 아니라 기존(`daily_brief.py`) — 확인만 하면 됨
> - **`자주 찾는 주제` 제거** — S4 잔여 작업으로 이월 (§S4)

지금 `feat/editorial-layer` 한 브랜치에 **성격이 다른 5덩어리**가 섞여 있다 — 선정 컷오프
(`9918e9c`) · 편집 override(`34f2b81`) · open_question(`f5042cb`) · 주간 판세(`cce4e85`) +
Phase 0 문서 3개. 명세 12장의 *"Phase별 브랜치 분리 / Phase 2는 독립 롤백 가능"* 과
이미 어긋나 있고, **다른 세션이 같은 워크트리에서 계속 커밋 중**이다.

### 해야 할 것

1. **병렬 세션 정지 또는 역할 분담 확정.** 같은 워크트리에 두 주체가 커밋하면 Phase별
   롤백이 성립하지 않는다. 이게 안 되면 아래는 전부 무의미하다.
2. `feat/editorial-layer`를 **통째로 main 병합**하고 거기서 다시 시작.
   - 커밋 단위로 쪼개는 대안이 있으나 권하지 않는다 — 4커밋 전부 테스트가 붙어 있고
     되돌릴 이유가 없다. 쪼개기 비용이 이득보다 크다.
   - **명세 12장의 브랜치 분리는 S2부터 지킨다**: `feat/p1-ranking` · `feat/p2-copy` · …
3. **명세 개정 5곳** (Phase 0 충돌 반영):

| 명세 위치 | 고칠 내용 |
|---|---|
| 4장 1-1 | `score_threshold_*`에 **`features` 결손 면제 규칙 추가**. 없으면 핵심 뉴스가 잘린다 |
| 4장 1-1 | `display_cap_*`는 신설이 아니라 **기존**(`daily_brief.py:59-60`) |
| 5장 2-2 | `btn.evidence` 행 삭제 — 그 문구는 트렌드 차트 토글이다. 선정 근거 버튼은 **신설** |
| 5장 2-3 | `state.empty_day`는 **이미 구현됨**(`app.js:634`). 문구 확정만 |
| 6장 | `이슈 아카이브`는 이미 별도 탭. "첫 화면에서 제거" 대상은 `자주 찾는 주제` 하나 |

> **산출**: 개정된 `NUCLENS_SPEC.md` v2. 이후 단계는 v2를 기준으로 한다.

---

## S1. features 결손 해소 — 명세에 없지만 최우선

### 왜 여기 있나

| 결손이 남으면 | 결과 |
|---|---|
| Phase 1-1 임계값 | 결손 면제로 덮은 채 14.0을 정하게 된다. **결손이 고쳐지면 분포가 바뀌어 재산출 필요** — 임계값을 두 번 정한다 |
| Phase 1-4 선정 사유 | 입력 factor가 실질 2값이라 템플릿을 3종으로 늘려도 재진술. `a64e252`가 되돌린 그 자리로 돌아간다 |
| 등급 신뢰도 | `must_read`의 40%가 LLM 판정이 아니라 출처로 자동 부여된 값 |

### 작업

1. **큐에 들어가기 전에 잡는다** ← 실효 지점 (구현 완료, `aa3315d`)

   `curation_errors()`(`data_quality.py:278`)가 **`features`를 검사하지 않아**(실측: 결손
   payload도 `[]` 반환) 결손 응답이 완결된 것으로 통과했다.

   **되돌릴 수 없게 되는 지점은 큐 적재다.** 기사는 큐에 들어가는 순간 `state["sent"]`에
   마킹되고 `article_seen()`(`news_bot.py:408`)이 재수집을 막는다 — 실측으로 결손 캐시
   20건 전부가 `sent`에 있다. 그 뒤에는 어떤 재큐레이션 경로로도 도달하지 못한다.

   **수정**: `curation_errors()`에 `require_features` 플래그 신설 → `curate_batch()`
   응답 검증에서만 켠다. 결손 응답은 재생성 대상이 된다.
   플래그를 기본값 False로 둔 이유: 아카이브 적재·배포 게이트는 '게시 자격' 판정이라
   features 없이도 통과해야 한다. 켜면 결손 기사가 아카이브에서 통째로 빠져
   트렌드·`prior_coverage` 재료가 사라진다.

   > 초판에서 "재큐레이션이 안 걸려 같은 10건이 매 회차 재등장한다"고 썼는데 **틀렸다.**
   > 반복 등장은 **큐 3일 보존**(`news_bot.py:1366`) 때문이다.

2. **폴백의 등급 자동 승격 중단** — 구현 완료 (`fallback_curation()`)
   - 기존: batch 실패 + `is_tier1_source()` → `must_read` 자동 부여, `features` 없음
   - 변경: 등급을 올리지 않는다. 이 레코드는 곧바로 `_legacy_score()`로 빠지므로
     승격은 "큐레이션이 실패한 1차 출처"를 `must_read`에 섞는 경로였다

3. **2차 방어선** — `needs_recuration()` 구현 완료
   `features` 결손도 재큐레이션 대상으로 본다. 위 `sent` 마킹 때문에 큐에 못 들어간
   격리분·`sent` 만료(14일)분에만 도달하는 보조 장치다. `FEATURES_RETRY_LIMIT=2`로
   무한 재질의를 막는다. **현재 캐시 697건 기준 재큐레이션 대상 순증 0건 — 추가 비용 없음.**
4. **결손률 로깅** — 지금은 흔적이 0이다.
   - crawl 로그 1줄 + `selection_stats`에 `features_missing` 필드 추가
   - 이게 없으면 고쳐졌는지 판단할 방법이 없다
5. **관찰 2주** — 신규 수집분 결손률 **5% 미만** 유지

### 수용 기준 (배포 후 관찰)

- [ ] 신규 수집분 `features` 결손률 < 5% (2주 연속)
- [ ] batch 실패가 `must_read`를 만들지 않음
- [ ] 결손률이 로그·`selection_stats` 양쪽에 남음
- [ ] `score_distribution.md` 재산출 시 `must_read` 이봉 분포가 단봉으로 수렴

### 배포 첫날 확인할 것

코드만으로는 성패를 알 수 없다. 배포 후 아래 셋을 눈으로 봐야 한다.

| 어디서 | 무엇을 | 나쁜 신호 |
|---|---|---|
| crawl 로그 마지막 줄 | `features_missing=N (x%)` | 비율이 이전 수준(10%+)에서 안 내려감 |
| crawl 로그 | `! 품질 게이트 재생성: N건` 빈도 | 매 chunk 재생성 = 프롬프트가 features를 못 받고 있음 |
| `delivery_log.jsonl` | `record_type:selection_stats` 줄의 `features_missing` | 지역별로 계속 0이 아님 |

`selection_stats`는 **아직 실제 로그에 0줄**이다(`AS_IS.md` §5) — 첫 `daily-brief` 실행이
이 계약의 첫 검증이다. 줄이 안 생기면 `append_selection_stats()` 경로부터 본다.

---

## S2. 랭킹 신뢰도 (명세 Phase 1) — 대부분 이미 구현됨

### 2-1 선정 하한 — `9918e9c`에 구현됨. 고칠 것 2가지

1. ✅ **등급 면제 제거** — `72ce421`. 면제는 `features` 결손 1종만 남았다.
   검증: 20회차 재점수화로 두 판정의 선정 집합 비교 → **139건 동일, 차집합 0**.
   `must_read`에도 14.0을 거는 더 엄격한 설정으로도 139건 그대로다(미달 `must_read`가
   전부 결손 면제에 걸린다).
2. ⏸ **임계값 재산출** — **S1 관찰 후에 한다.** 결손이 줄면 legacy 봉우리가 사라져
   분포가 통째로 바뀐다. 지금 재산출하면 두 번 하게 된다.
   `scratchpad/extract_pool.py` → `analyze_pool.py` 재실행. 목표는 그대로 **주 1~2회 0건**

### 2-2 에디터 픽 0~2건

명세는 **자동** 0~2건을 요구한다. 그런데 `34f2b81`이 **수동** `selection_overrides.json`을
이미 넣었다. 역할이 겹친다.

> **결정 필요**: 자동 에디터 픽을 만들 것인가, 수동 override로 갈 것인가, 둘 다인가.
> 둘 다면 우선순위 규칙(수동이 자동을 이기는가)을 정해야 한다.

### 2-3 사건 단위 병합 — 확인만

이미 있다: 임베딩 0.92 + 시설·국가 충돌 차단(`build_data.py:943`) + 회색지대 LLM
검수(`issue_review.py`). 명세가 요구한 것 중 확인할 것:
- 대표 기사 선택 규칙이 **공식 원문 > 1차 보도 > 후속 보도** 순인가
- 병합 근거가 데이터로 남는가 → `issue_audit.json`에 있음. 화면 노출 여부만 결정

### 2-4 선정 사유 — **품질 게이트 통과 전 착수 금지**

"S1 끝나면"으로만 두면 Phase 1에서 다시 주관적으로 판단하게 된다. **수치 게이트**로 박는다
(값은 확정이 아니라 착수 전 승인 대상):

| 게이트 | 기준 | 왜 |
|---|---|---|
| features 결손률 | 고유 기사 기준 **< 10%** | 입력이 없으면 사유도 없다 |
| feature 값 편중 | 어느 feature도 단일 값이 **< 75%** | 지금 `korea_relevance` 58.7% · `report_worthiness` 62% |
| 사유↔분류 중복률 | 생성 사유 문구와 `category`·`event_type` 라벨의 단순 중복 **< 50%** | `a64e252`가 되돌린 실패 모드 |
| 상위 조합 집중 | 최빈 사유 조합이 전체의 **< 15%** | 실측 '정책 결정/정책 영향 큼' 22% |
| legacy 식별 | legacy 경로 항목이 상태 화면·로그에서 구분 가능 | 사유를 만들 수 없는 항목을 조용히 통과시키지 않기 |

통과하면 규칙 기반 3종 템플릿 + 직전 2일 회피를 구현한다.

---

## S3. 문구 교체 (명세 Phase 2) — 중립분만, S0 직후 병행 가능

### 범위: 786건 중 **표시 문구 412건만** (`index.html` 135 + `app.js` 277)

`build_data.py`의 **374건은 이번 범위 밖**이다. 빌드 시점에 JSON에 구워지므로 고쳐도
재빌드 전엔 화면에 안 뜬다 — 명세 2-1의 목적("문구 수정에 배포 불필요")을 이 층에서는
달성할 수 없다. 별도 과제로 남긴다.

### 작업

1. `copy.json` 신설 + 412건 이관. 각 항목에 `tone` · `fallback_neutral`
2. 명세 2-2 섹션명·버튼 교체 (`btn.evidence` 제외 — S0에서 명세 정정)
3. 명세 2-3 상태 문구 중 **중립 결만** — `empty_day`(문구 확정) · `official` · `error` ·
   `no_result` · `not_ready`
4. AI 상투어 필터 `style_rules.json` + 후처리 재생성 2회 → 실패 시 문장 절단

**유머 결 4종(`quiet` · `no_change` · `surge` · `loading`)은 S6으로 미룬다.** 이유는 아래.

### 수용 기준

- [ ] UI 문자열 412건이 외부 파일로 분리
- [ ] 최근 14일 요약문에서 금지 표현 검출 0건
- [ ] S3 커밋만 되돌려도 서비스 정상 동작

---

## S4. 첫 화면 재구성 (명세 Phase 3) — 진행 중

> **2026-08-03 진행분.** 상세·근거·게토차는
> [`2026-08-03-signal-desk-handoff.md`](2026-08-03-signal-desk-handoff.md) 에 있다.
> 여기는 슬롯 단위 체크리스트만 둔다.

| 항목 | 상태 | 커밋 |
|---|---|---|
| 딥 포레스트 팔레트 | ✅ | `d6621aa` |
| 레이아웃·타이포그래피 (표 4열·대형 헤드라인·모노 상태바) | ✅ | `26c1d3e` `7ee41d7` `76b3382` |
| 우측 상시 근거 패널 | ✅ | `4063caa` |
| 발간물 탭 정리 (29→18건·기관명 약자) | ✅ | `32528c4` |
| 화면 배포 자동화 | ✅ | `cb5b7bf` `9e2efaa` |
| **이슈 지도 (Atlas)** | ❌ 데이터 대기 — 착수 조건은 이제 `meta.atlas_readiness` 가 매 빌드 계산한다 | — |
| **`자주 찾는 주제` 제거** | ✅ 아카이브 주제 필터와 완전 중복이었다 | `c9b5656` |
| **주간 판세 블록 위치 결정** | ✅ **현 위치(주간 흐름 탭) 유지로 확정** — 코드 변경 없음 | — |
| **`open_question` vs `next_checkpoint` 중복 정리** | ⏸ 대기 — 값이 0건이라 판단 불가 | — |
| **⚠️ 주간 판세가 3개월간 생성된 적 없다** | ❌ **신규 차단 요인** — 아래 참조 | — |

**S4 에 남은 사용자 결정은 0건이다.** 남은 것은 데이터가 차기를 기다리는 항목뿐.

판단 근거 (없는 명세를 근거로 쓰지 않기 위해 여기 적는다):

- **주간 판세 블록**(`cce4e85`, `#weeklyReport`) — 2026-08-03 확정: **지금 자리가 맞다.**
  후보 3안을 샘플 데이터로 실제 렌더해 홈 전체를 같은 폭·배율로 대조했다:

  | 안 | 홈 높이 | 대가 |
  |---|---|---|
  | **A. 주간 흐름 탭 (현재)** | 2,403px | 없음 |
  | B. 홈 히어로 아래 | 3,127px (+724) | 오늘 이슈가 한 화면 아래로 밀린다 |
  | C. 홈 사이드바 | 2,693px | 360px 칸에서 블록이 724→1,005px, 사이드가 본문보다 길어짐 |

  세 기준이 같은 쪽을 가리킨다 — ① 이 블록은 **금요일에만** 생기는데 홈은 매일 본다
  (주 6일은 빈자리) ② 홈은 `TODAY`, 주간 흐름은 `THIS WEEK` 로 라벨 체계가 이미 갈려
  있다(`index.html` 주석에 "같은 시간 범위에 라벨을 두 번 다는 건 잡음") ③ 5섹션 산문을
  360px 에 넣으면 한 문장이 3~4줄로 끊긴다.
  **결론: 코드를 옮기지 않는다. 이 항목은 '미결'이 아니라 '현 위치가 맞다고 판정됨'이다.**

- **⚠️ 그런데 이 블록은 한 번도 화면에 뜬 적이 없다** (2026-08-03 실측):

  ```
  weekly_reports.json          워킹트리 없음 · 전 브랜치 git 이력 0건 · gitignore 대상 아님
  .github/workflows/weekly.yml 2026-05-08 도입 → 금요일 12회 지남
  "chore: weekly report" 커밋   0건
  ```

  **코드 결함이 아니다.** 로컬에서 `load_curated → get_week_articles(126건) →
  batch_synthesize → save_weekly_report` 를 끝까지 돌리면 `2026-W32` 파일이 정상
  생성된다. 조기 반환(`weekly_bot.py:413`)도, `batch_synthesize` 예외도 걸리지 않는다.
  → **워크플로 실행 쪽 문제.** private repo 라 Actions 이력은 저장소 밖에서 못 본다.
  **다음 할 일: GitHub → Actions → "Weekly report" 실행 이력 확인.** 0건이면 스케줄이
  안 걸린 것, 빨간불이면 로그에 답이 있다.

  곁다리: `count_unique_issues`(`weekly_bot.py:309`)가 126건 입력에 126을 돌려준다.
  제목 앞 40자 정규화로 세는데 상류에서 완전일치 제목을 이미 걷어낸 뒤라 아무것도
  안 걸린다 — "후속 보도 많은 주 = 풍성한 주" 착시를 막으려던 함수가 실질 no-op.

- **이슈 지도(Atlas) 착수 조건은 이제 코드가 잰다.** `meta.atlas_readiness`
  (`build_data.atlas_readiness`)가 매 빌드 5개 노드 보유율·차단 노드·`ready` 를
  계산하고, 빌드 로그에 `[build_data:atlas]` 한 줄로 찍는다. 산문으로만 두면 매
  세션이 다시 재고, 실제로 이 문서와 인계 문서가 서로 다른 숫자를 들고 있었다.

  **게이트가 아니라 계기판이다** — 오늘 추적률을 배포 게이트로 썼다가 뉴스가 한산한
  날 CSS 오타 수정까지 막힌 일이 있다. 테스트는 배관만 검사하고 수치는 안 건다.
  문턱(`ATLAS_MIN_OPEN_QUESTION` 1건 · `ATLAS_MIN_RELATED_RATE` 0.20)을 조용히
  낮추지 못하게 잠그는 테스트는 걸어뒀다.

  2026-08-03 20시 실측(로컬 폴백 벡터 빌드, 이슈 119): `latest_change` 4(3%) /
  `open_question` 0(0%) / `implication` 72(60%) / `related_articles` 8(7%) /
  `official_source` 7(6%) — 5칸 0건, 3칸+ 5건, 차단 `open_question`·`related_articles`.
  라이브(원격 임베딩, 이슈 110)에서는 6 / 0 / 68 / 18 / 7 이다. 로컬이 낮은 것은
  임베딩 캐시가 없어 병합이 덜 되기 때문이고 환경 차이지 회귀가 아니다.

  **네 노드가 두 뿌리로 수렴한다**: `related_articles`·`latest_change` 는 병합
  판정기(2026-08-03 밴드 0.84 확장), `open_question` 은 게이트(같은 날 사유별 계측
  추가). `official_source` 는 출처 구성의 성질이라 구조적 상한이 있다.

- **`open_question`**(`f5042cb`) 과 `next_checkpoint` 는 자리가 겹친다. 다만
  `open_question` 은 **현재 0/110 이라 화면에 한 번도 뜬 적이 없다** — 중복 정리는
  값이 실제로 생성되기 시작한 뒤에 판단하는 게 맞다(S4 잔여가 아니라 대기 항목).
- ~~**`자주 찾는 주제` 제거**는 판단이 필요 없다. 그냥 하면 된다.~~ → `c9b5656` 완료.
  실측 근거: 이 칩은 눌러도 아카이브 탭의 주제 필터로 갈 뿐인데, 그쪽엔 같은 필터가
  건수까지 붙어 19개 있다(사이드는 8개·건수 없음). 목적지가 같고 정보량이 적었다.

---

## S5. 민감도 제어 (명세 Phase 4) — 저장소에 개념 자체가 없다

`sensitivity_level`은 **전체 검색 0건**이다. 명세는 스키마만 쓰면 되는 것처럼 읽히지만
실제로는 3가지가 따라온다:

1. 큐레이션 프롬프트에 등급 판정 추가 (`news_bot.py:968` items 스키마)
2. 아카이브 **568건 백필** — 없으면 과거 날짜 화면에서 등급이 전부 누락 → 기본값 3(전체
   중립)으로 떨어져 유머가 영영 안 켜진다
3. 렌더 파이프라인 강제 + 등급 0~3 단위 테스트

**누락 시 기본값 3(전체 중립)** 은 명세대로 유지한다 — 안전한 쪽으로 실패해야 한다.

---

## S6. 유머 문구 활성화 — S5 통과 후 1시간

명세는 Phase 2에서 유머 문구를 넣고 Phase 4에서 통제하라고 한다. **순서를 뒤집는다.**

명세 P5는 *"유머는 코드로 통제한다 — 민감도 등급에 따라 자동 비활성화"* 인데,
`sensitivity_level`이 없는 상태에서 유머를 켜면 **자동 비활성화가 구조적으로 불가능**하다.
사고·인명 피해 기사 옆에 "오늘은 조용합니다"가 뜨는 사고가 Phase 2~4 사이 내내 열려 있다.

S5가 끝나면 `copy.json`의 `tone: "humor"` 4건을 켜고 화면당 상한 2개를 적용한다.
`fallback_neutral`은 S3에서 이미 다 채워져 있으므로 스위치만 켜는 작업이다.

---

## S7. 사건 추적 (명세 Phase 5) — 별도 트랙

착수 전 **데이터 출처를 먼저 정해야 한다.** `web/public/data/`는 gitignore라 어제가 없다
(`AS_IS.md` C7). 두 선택지:

| 안 | 방법 | 비용 |
|---|---|---|
| A | 매 빌드에 어제 이슈를 `archive`+`delivery_log`에서 재구성해 비교 | 빌드 시간 증가, 병합 결과가 흔들리면 델타도 흔들림 |
| B | 이슈 클러스터 결과를 커밋되는 파일로 저장(사건 ID 지속성) | 명세 5-1이 요구하는 것과 동일. 초기 비용 크지만 정답 |

**B 권고.** A는 5-1(사건 ID 지속성)을 어차피 다시 하게 된다.

명세 5-3 사실성 분류(`status` 5종 · `confirmation_basis`)는 현재 `verification_state()`의
4단계와 **다른 축**이다 — 현재 것은 출처 개수 기반이지 사실성 분류가 아니다. 둘을 합칠지
분리할지 결정 필요. `open_question`(`f5042cb`)이 `uncertainty_note` 자리를 일부 선점했다.

---

## 지금 결정해야 할 것 5가지

1. **병렬 세션** — 같은 워크트리 동시 작업을 멈출 것인가, 역할을 나눌 것인가 (S0 차단)
2. **S1 승격 승인** — features 결손을 Phase 1 앞에 넣는 데 동의하는가. 명세 범위 밖이지만
   원인이 `curation_errors()` 한 줄로 좁혀졌다
3. **에디터 픽** — 자동(명세 1-2) / 수동(`selection_overrides.json`) / 둘 다
4. **문구 범위** — `build_data.py` 374건을 이번에 건드릴 것인가 (권고: 아니오)
5. **유머 연기** — 유머 문구를 S5 이후로 미루는 데 동의하는가 (권고: 예)
