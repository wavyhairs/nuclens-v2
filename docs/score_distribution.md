# 선정 점수 분포 (Phase 0)

`NUCLENS_SPEC.md` Phase 0 추가 산출물. 명세 1-1이 요구한 임계값을 **실제 분포에서** 산출하기
위한 재료다.

> ⚠️ **`NUCLENS_SPEC.md` 는 존재하지 않는다**(2026-08-03 확인 — git 전 이력 0건).
> 정본은 [`PHASE_PLAN.md`](PHASE_PLAN.md) 다. 이 문서 안의 "명세 N-M" 참조는
> 복원 불가이므로 근거로 쓰지 말 것.

**요약**: 점수는 중요도의 단조 함수가 아니다 — **`features` 결손 여부에 따라 두 척도가 섞인
값**이다. 그래서 명세의 절대 점수 하한을 *그대로* 걸면 중요한 뉴스가 잘린다(§7). 다만
**결손 항목 면제 한 줄이면 명세 형태 그대로 쓸 수 있고**, 현재 구현된 등급 기반 하한과
선정 결과가 완전히 같다(§7-2). 결론은 "절대 하한을 버려라"가 아니라 **"면제 규칙을
넣어라"**다.

- 표본: **20회차 / 19일** (2026-07-14 ~ 2026-08-01)
- 후보: 큐 원본 481건 → noise·market 제외 **463건** → 중복 클러스터 통과 **433건**
- 등급(중복 통과분): `must_read` 88 / `nice_to_know` 345

### ⚠️ 표본 단위 — 아래 모든 표는 **회차별 관측치**다

| 기준 | 수 |
|---|---:|
| 관측치 (회차 × 기사) | **463** |
| 고유 기사 | **269** |
| 평균 등장 회차 | 1.72 |
| 2회 이상 등장한 기사 | 120건 (최대 4회) |

큐는 3일 컷(`news_bot.py:1366`)이라 **한 기사가 최대 4회차까지 후보로 남는다.** 선정은
회차마다 독립적으로 일어나므로 **하한·컷오프 판단에는 관측치가 맞는 단위**다. 다만
"데이터 품질이 얼마나 나쁜가"를 말할 때는 고유 기사 기준이 맞다 — §4에 둘 다 적었다.
표를 인용할 때 어느 단위인지 반드시 같이 적을 것.

재현: `scratchpad/extract_pool.py` → `analyze_pool.py`.

---

## 0. 표본을 어떻게 만들었나

`delivery_log.jsonl`은 **선정된 항목만** 담는다(`daily_brief.append_delivery_log()`). 하한을
실험하려면 떨어진 후보까지 있어야 하므로 쓸 수 없다.

대신 `digest_queue.json`의 커밋 이력을 썼다. `chore: daily brief claim` 커밋은 선정 직후
(큐에서 발송분 제거) 상태이므로, **그 부모 커밋의 큐 = 선정 직전의 전체 후보 풀**이다.

```bash
git log --format='%H %cI' --grep='daily brief claim' origin/main
git show <claim>^:digest_queue.json
```

### 회차 단위로 센 이유

2026-07-14는 13:11·23:15 두 번 돌았다. 뒤 회차의 큐는 앞 회차가 소진하고 남은 것이라 둘은
같은 풀이 아니고, 어느 한쪽을 버리면 그날 실제로 내려간 선정이 왜곡된다. **20회차를 전부
독립 선정 이벤트로 센다.** 아래 "0건 회차"는 날이 아니라 회차 기준이다.

### 재현 한계

- `prior_coverage`(tracking 가점)는 2026-08-01 `4da745d` 도입 — 대부분 회차에 필드가 없어
  **tracking=0 · novelty=2로 보수 재현**된다. 실제 운영 점수는 후속 보도에서 최대 +1.5 높다.
- `time_decay` 기준 시각은 claim 커밋 시각을 썼다.
- 20회차는 표본이 적다. 아래 값은 정답이 아니라 **첫 후보값**이며 배포 후 재조정 전제.

### 기존 백테스트 문서와의 차이

`docs/2026-08-03-selection-floor-backtest.md`의 결론(등급 기반 하한)은 그대로 재현됐다.
다만 두 곳이 다르다 — 이 문서 값이 맞다.

| 항목 | 기존 문서 | 이 문서 | 원인 |
|---|---|---|---|
| 지역별 분포 표 | "중복 통과분" 국내 111·해외 352 | 같은 수치이나 **중복 제거 전** | 기존 문서 레이블 오류. 중복 통과분은 국내 96·해외 337 |
| 표본 | 19일(7/14 재실행 1건 제외) | 20회차 | 제외한 쪽에 그날 국내 must_read가 몰려 있었음 |

---

## 1. 지역별 점수 분포

중복 클러스터 통과분(433건):

| 지역 | n | min | P25 | 중앙 | P75 | max |
|---|---:|---:|---:|---:|---:|---:|
| 국내 | 96 | 2.0 | 10.3 | 13.0 | 18.1 | 29.2 |
| 해외 | 337 | 2.0 | 10.5 | 13.4 | 16.4 | 27.6 |
| 전체 | 433 | 2.0 | 10.4 | 13.4 | 16.5 | 29.2 |

중복 제거 전(463건):

| 지역 | n | min | P25 | 중앙 | P75 | max |
|---|---:|---:|---:|---:|---:|---:|
| 국내 | 111 | 2.0 | 9.9 | 12.7 | 18.2 | 29.2 |
| 해외 | 352 | 2.0 | 10.6 | 13.4 | 16.4 | 27.6 |

**국내와 해외의 분포는 거의 같다.** "국내가 `source_bonus`에서 구조적으로 불리하니 임계값을
낮춰야 한다"는 가설은 이 표에서 근거가 없다. 명세가 임계값을 지역별로 나눈 것은 실제 분포
차이가 아니라 캡이 지역별로 나뉜 것의 관성이다.

---

## 2. 등급별 분포 — 여기서 문제가 드러난다

| 등급 | n | min | P25 | 중앙 | P75 | max |
|---|---:|---:|---:|---:|---:|---:|
| must_read | 88 | 7.7 | **9.7** | **22.6** | 25.1 | 29.2 |
| nice_to_know | 345 | 2.0 | 10.6 | 13.3 | 15.4 | 21.8 |

`must_read`는 P25 9.7 / 중앙 22.6 — **이봉(bimodal)이다.** 9~10점 무리와 22~25점 무리로
갈려 있고, 그 결과 **`must_read`의 상당수가 `nice_to_know` 중앙값보다 낮다.**

## 3. 히스토그램 (2점 구간, 중복 통과분)

| 구간 | 전체 | must_read | nice_to_know |
|---|---:|---:|---:|
| 2–4 | 13 | 0 | 13 |
| 4–6 | 15 | 0 | 15 |
| 6–8 | 15 | 8 | 7 |
| 8–10 | 56 | 22 | 34 |
| 10–12 | 70 | 5 | 65 |
| 12–14 | 70 | 2 | 68 |
| 14–16 | 72 | 0 | 72 |
| 16–18 | 35 | 0 | 35 |
| 18–20 | 24 | 2 | 22 |
| 20–22 | 18 | 4 | 14 |
| 22–24 | 14 | 14 | 0 |
| 24–26 | 22 | 22 | 0 |
| 26–28 | 8 | 8 | 0 |
| 28–30 | 1 | 1 | 0 |

`must_read` 88건 중 **35건이 12점 미만**이고, 그 구간(6~12)은 `nice_to_know` 106건과 겹친다.
12~22 구간에는 `must_read`가 거의 없다 — 골(valley)이다.

---

## 4. 진범 — features 결손

단위에 따라 수가 크게 달라진다. **둘 다 맞는 수치이고, 쓰임이 다르다.**

| 등급 | 단위 | 전체 | features 없음 | 비율 |
|---|---|---:|---:|---:|
| must_read | 관측치 | 91 | 37 | **40.7%** |
| must_read | **고유 기사** | 68 | 16 | **23.5%** |
| nice_to_know | 관측치 | 372 | 31 | 8.3% |
| nice_to_know | **고유 기사** | 201 | 15 | 7.5% |
| 합계 | 관측치 | 463 | 68 | 14.7% |
| 합계 | **고유 기사** | 269 | 31 | 11.5% |

- **선정·하한 논의**에는 관측치를 쓴다 — 회차마다 독립적으로 선정되므로 결손 항목이
  여러 회차에 걸쳐 반복 후보가 되는 것 자체가 문제의 크기다.
- **데이터 품질 논의**에는 고유 기사를 쓴다 — `must_read`의 **23.5%**가 features 없이
  큐레이션됐다는 뜻이다.
- 관측치 비율이 더 높은 이유는 **큐가 3일 보존**(`news_bot.py:1366`)이기 때문이다.
  선정되지 못한 항목은 만료까지 매일의 스냅샷에 다시 나온다. 결손 항목은 점수가 낮아
  거의 선정되지 않으므로 3회차를 꽉 채우고, 그만큼 관측치 비율이 부풀려진다.

`ranking.sanitize_features()`가 None을 반환하면 `score_item()`이 `_legacy_score()`로 빠진다
(`ranking.py:154`). 이 경로는 `base 10.0(must_read) + khnp 2.0 + 1차도메인 2.0 + 보고서 1.0`
뿐이라 **`event_weights`도 feature 가중치도 전혀 반영되지 않는다.** `must_read`는 사실상 10점
근처에 고정되고, 여기에 `time_decay`만 깎여 7.7~11.7 사이로 흩어진다.

즉 위 이봉 분포의 왼쪽 봉우리는 **중요도가 낮은 기사가 아니라 큐레이션이 실패한 기사**다.

### 결손 항목의 도메인 분포

| 도메인 | 결손 | 해당 도메인 전체 | 결손률 |
|---|---:|---:|---:|
| `news.google.co.kr` | 16 | 121 | 13.2% |
| `reuters.com` | 12 | 31 | **38.7%** |
| `ans.org` | 6 | 74 | 8.1% |
| `globenewswire.com` | 5 | 17 | 29.4% |
| `world-nuclear-news.org` | 4 | 95 | 4.2% |
| `khnp.co.kr` | 3 | 3 | 100.0% |
| `corridorbusiness.com` | 3 | 3 | 100.0% |
| `canarymedia.com` | 3 | 9 | 33.3% |
| `knau.org` · `theglobeandmail.com` · `maritime-executive.com` · `thebulletin.org` | 각 3 | 각 3 | 100.0% |

### 결손 항목의 날짜 분포 — 상시가 아니라 **배치 단위로 터진다**

| 날짜 | 결손 | 전체 | 결손률 |
|---|---:|---:|---:|
| 2026-07-14 | 14 | 23 | 60.9% |
| 2026-07-15 | 2 | 31 | 6.5% |
| 2026-07-16 | 2 | 26 | 7.7% |
| 2026-07-17 ~ 07-26 | 0 | 210 | **0.0%** |
| 2026-07-27 | 10 | 22 | 45.5% |
| 2026-07-28 | 10 | 27 | 37.0% |
| 2026-07-29 | 10 | 28 | 35.7% |
| 2026-07-30 | 0 | 25 | 0.0% |
| 2026-07-31 | 10 | 41 | 24.4% |
| 2026-08-01 | 10 | 40 | 25.0% |

10일 연속 0%였다가 특정 날 10건씩 정확히 터진다. **batch 큐레이션 chunk 단위 실패**이지
개별 기사 특성이 아니다. 7/27~29와 7/31~8/1이 각각 *같은 10건*인 이유는 **큐 3일 보존**
(`news_bot.py:1366`) — 선정되지 못한 항목이 만료까지 매일 다시 후보로 올라온다. 결손
항목은 점수가 낮아 거의 선정되지 않으므로 3회차를 꽉 채운다.

> **원인 규명 (2026-08-03 정정)**: 결손이 되돌릴 수 없게 되는 지점은 **큐 적재**다.
> 기사는 큐에 들어가는 순간 `state["sent"]`에 마킹되고 `article_seen()`(`news_bot.py:408`)이
> 재수집을 막으므로, 그 뒤에는 어떤 재큐레이션 경로로도 도달할 수 없다(실측: 결손 캐시
> 20건 전부가 `sent`에 있다). 따라서 막아야 할 곳은 **`curate_batch()` 응답 검증**이다.
> `curation_errors()`가 `features`를 안 보던 탓에 결손 응답이 완결된 것으로 통과했다.
> 하한 도입과 **독립적인 데이터 품질 결함**이며, [`PHASE_PLAN.md`](PHASE_PLAN.md) S1이다.

---

## 5. feature 값 분포 — 변별력이 있는가

features 있는 395건 기준. `score_item()`이 실제로 쓰는 값이다.

| feature | 0 | 1 | 2 | 3 | 최빈값 비중 | 판정 |
|---|---:|---:|---:|---:|---:|---|
| `korea_relevance` | 45 | 232 | 47 | 71 | 58.7% | 편중 |
| `market_materiality` | 53 | 161 | 168 | 13 | 42.5% | 분산 |
| `policy_materiality` | 20 | 173 | 154 | 48 | 43.8% | 분산 |
| `novelty` | 0 | 0 | 395 | 0 | **100.0%** | 상수 |
| `evidence_strength` | 9 | 100 | 214 | 72 | 54.2% | 편중 |
| `report_worthiness` | 88 | 245 | 57 | 5 | 62.0% | 편중 |

- `novelty`는 `prior_coverage` 부재로 기본값 2에 고정 — 다만 **가중치가 0**이라 점수에
  기여하지 않는다(`ranking_config.json:28`). 설계대로다.
- `report_worthiness`는 **`feature_weights`에 키 자체가 없어 점수 기여 0**이다. 버그가 아니라
  용도가 다르다 — `daily_brief.py:343`의 보고서 검토 추천 게이트와 `weekly_bot.py:131`이
  쓴다. 위 표에 함께 실었지만 **랭킹 변별력 논의에서는 빼고 읽을 것.**
- `market_materiality`·`policy_materiality`는 사실상 1과 2의 이진 선택이다(둘이 83~84%).
- 3점을 거의 안 주는 것이 공통 패턴이다. 앵커 없는 절대평가라 중앙으로 수렴한다.

**Phase 1-4(선정 사유)에 직접 걸리는 사실**: 사유 문장의 입력이 되는 factor가 실질 2값이면,
템플릿을 3종으로 늘려도 문장 조합 수는 늘지 않는다.

## 6. breakdown 항목별 기여도 — **두 모집단을 분리해서 본다**

중복 통과분 433건 = **정상 features 368** + **legacy 경로 65**. 두 경로는 아예 다른 항목
집합을 쓰므로(legacy는 `event_weights`·feature 가중치를 안 탄다) 섞어서 표준편차를 내면
정상 후보의 변별력이 희석된다. 미등장 항목은 해당 모집단 안에서 0으로 채웠다.

### A. 정상 features 후보 (n=368)

| 항목 | 등장 | 등장률 | 평균 | 표준편차 | 값 범위 |
|---|---:|---:|---:|---:|---|
| `event:policy_decision` | 71 | 19% | 1.16 | **2.37** | 6.0 고정 |
| `event:regulatory_action` | 34 | 9% | 0.55 | **1.74** | 6.0 고정 |
| `importance` | 368 | 100% | 5.69 | **1.73** | 5.0 / 10.0 |
| `event:corporate_move` | 100 | 27% | 0.82 | **1.33** | 3.0 고정 |
| `event:project_milestone` | 33 | 9% | 0.36 | **1.14** | 4.0 고정 |
| `korea_relevance` | 327 | 89% | 1.61 | **1.07** | 1.2~3.6 |
| `event:incident_safety` | 8 | 2% | 0.13 | **0.87** | 6.0 고정 |
| `evidence_strength` | 336 | 91% | 1.59 | **0.80** | 1.0~3.0 |
| `time_decay` | 368 | 100% | −0.95 | **0.77** | −3.0~0 |
| `source_tier2` | 186 | 51% | 0.76 | **0.75** | 1.5 고정 |
| `market_materiality` | 322 | 88% | 1.37 | **0.74** | 1.0~3.0 |
| `policy_materiality` | 354 | 96% | 1.58 | **0.74** | 1.0~3.0 |
| `source_tier1` | 20 | 5% | 0.16 | **0.68** | 3.0 고정 |
| `event:research_report` | 38 | 10% | 0.21 | **0.61** | 2.0 고정 |
| `event:market_signal` | 19 | 5% | 0.10 | **0.44** | 2.0 고정 |
| `event:contract_award` | 1 | 0% | 0.02 | **0.31** | 6.0 고정 |
| `tracking:follow_up` | 2 | 1% | 0.01 | **0.11** | 1.5 고정 |
| `event:other` | 4 | 1% | 0.01 | **0.10** | 1.0 고정 |
| `event:opinion` | 60 | 16% | 0.00 | **0.00** | 0.0 고정 |

최종 점수: min 6.1 / P25 11.6 / 중앙 **14.3** / P75 17.6 / max 29.2

### B. legacy 경로 후보 (n=65) — 항목이 4개뿐이다

| 항목 | 등장 | 등장률 | 평균 | 표준편차 | 값 범위 |
|---|---:|---:|---:|---:|---|
| `importance` | 65 | 100% | 7.85 | **2.48** | 5.0 / 10.0 |
| `time_decay` | 65 | 100% | −1.13 | **0.79** | −3.0~0 |
| `primary_domain` | 7 | 11% | 0.22 | **0.62** | 2.0 고정 |
| `khnp_section` | 4 | 6% | 0.12 | **0.48** | 2.0 고정 |

최종 점수: min 2.0 / P25 4.4 / 중앙 7.7 / P75 9.7 / max **13.7**

### 분리해서 보니 드러나는 것

**두 모집단의 점수 구간이 거의 겹치지 않는다** — legacy 최대 13.7 < 정상 중앙 14.3.
§2에서 "`must_read`가 이봉"이라고 쓴 것의 실체는 등급이 아니라 **경로**다. 하나의 축에
두 개의 다른 척도가 얹혀 있다.

그래서 채택된 하한 **14.0은 legacy 상한(13.7) 바로 위**에 놓인다. 면제가 없으면 legacy
전량이 잘리고, 면제가 있으면 legacy 전량이 무조건 통과한다. **하한은 정상 후보에만
작동하고 있다.** 이것이 §7-2에서 두 설계가 동일한 결과를 낸 이유이기도 하다.

**정상 후보 안에서 순위를 가르는 것은 `event_type`과 `importance` 둘뿐이다.** 상위 5개 중
4개가 `event_type` 분기이고, LLM feature 3개(korea/policy/market)를 다 합쳐도 `importance`
하나의 변별력에 못 미친다.

### 전 표본에서 한 번도 발동하지 않은 항목

| 항목 | 등장 | 왜 |
|---|---:|---|
| `related_reports` (`related_reports_bonus` 1.0) | **0회** | `reports_kb.json` 미구성(`.example`만 있음) |
| `tracking:repeat` (0.5) | **0회** | `prior_coverage > 2`가 표본에 없음 |
| `tracking:follow_up` (1.5) | 2회 | `prior_coverage`가 2026-08-01 도입 |
| diversity penalty (−2.5) | — | `breakdown`에 안 남는다. `select_diverse()`가 점수 구성이 아니라 **선별 단계**에서 적용 |

`cluster_duplicates()`도 점수 구성요소가 아니라 후보 집합 변환이다 — 위 표에 없는 것이 맞다.

---

## 7. 명세 1-1의 절대 점수 하한 — 시뮬레이션

`must_read 탈락` = 하한이 없을 때 선정되던 `must_read` 중 하한 때문에 빠진 건수(분모 57).

| floor | 국내 0건 회차 | 국내 평균 | 해외 0건 회차 | 해외 평균 | 총 선정 | must_read 탈락 |
|---:|---:|---:|---:|---:|---:|---|
| 없음 | 0 | 2.9 | 0 | 5.6 | 169 | 0/57 (0.0%) |
| 9 | 1 | 2.8 | 0 | 5.5 | 164 | 0/57 (0.0%) |
| 10 | 1 | 2.6 | 0 | 5.3 | 159 | 3/57 (5.3%) |
| 12 | 3 | 2.1 | 3 | 5.1 | 145 | **6/57 (10.5%)** |
| 14 | 4 | 1.8 | 3 | 4.7 | 128 | 7/57 (12.3%) |
| 16 | 5 | 1.4 | 5 | 3.8 | 102 | 7/57 (12.3%) |

**딜레마**: `must_read` 탈락 0%를 지키는 하한(≤9)에서는 0건 회차가 20회 중 1회뿐이라
컷오프가 사실상 작동하지 않는다. 명세의 목표("주 1~2회 0건")를 만족하는 하한(≥12)에서는
`must_read`가 10% 이상 잘린다.

### floor=12 에서 잘리는 must_read — **35건 전부가 features 결손**

```
2026-07-14 [국내]  9.6  정부, 메가 프로젝트 전력 공급 위해 원전 및 SMR 추가 공식화
2026-07-14 [국내]  9.9  기후장관, "원전·SMR 공론화 거쳐 전기본에 반영" 신규 원전 추진 공식화
2026-07-14 [국내] 11.6  한빛 1·2호기, 7년 만에 트립 결함 해소…계속운전 청신호
2026-07-29 [국내] 11.7  (KHNP 공지)
2026-07-28 [해외]  8.7  Canada plans to build 10 new nuclear reactors …
2026-07-31 [해외]  9.6  ARC partners with INL for first reactor deployment
    … 총 35건, features 결손 35건 (100%)
```

같은 `한빛 1·2호기` 이슈가 하루 뒤(7/15) features를 제대로 받았을 때는 **25.9점**이다.
동일 사건이 큐레이션 성공 여부에 따라 11.6 ↔ 25.9로 갈린다.

**결론: 절대 하한은 "중요하지 않은 기사"가 아니라 "큐레이션이 실패한 기사"를 자른다.**
그리고 그 실패는 로그에 아무 흔적을 남기지 않는다. 조용히 틀리는 종류의 실패다.

---

## 7-2. 면제 규칙을 넣으면 절대 하한이 살아난다 — §7 결론의 정정

§7은 **면제 규칙 없이** 절대 하한을 건 경우다. `features` 결손 항목 하나만 면제하면
결과가 완전히 달라진다.

| floor | 국내 0건 회차 | 국내 평균 | 해외 0건 회차 | 해외 평균 | 총 선정 | must_read 탈락 |
|---:|---:|---:|---:|---:|---:|---|
| 10 | 0 | 2.8 | 0 | 5.5 | 165 | **0/57** |
| 12 | 1 | 2.5 | 2 | 5.2 | 154 | **0/57** |
| 13 | 2 | 2.2 | 2 | 5.2 | 148 | **0/57** |
| **14** | **2** | **2.1** | **2** | **4.8** | **139** | **0/57** |
| 16 | 3 | 1.7 | 4 | 4.3 | 121 | **0/57** |
| 18 | 5 | 1.6 | 4 | 3.9 | 108 | **0/57** |

`must_read` 탈락이 전 구간 0이다. §7에서 잘리던 35건이 **전부 features 결손**이었으므로
(§7 참조), 그 면제 하나가 문제를 통째로 없앤다.

### 두 설계는 이 표본에서 구분되지 않는다

| 설계 | 국내 0건 | 해외 0건 | 총 선정 | must_read 탈락 | nk 비중 |
|---|---:|---:|---:|---:|---:|
| 등급 기반 14 (채택안) | 2 | 2 | 139 | 0 | 58% |
| 절대 14 + 결손 면제 (명세형) | 2 | 2 | 139 | 0 | 58% |

**선정 결과가 항목 단위로 완전히 같다** — 공통 139건, 차집합 0건.

이유는 하나다:

```
features 있는 must_read 중 14점 미만: 0건
features 결손 must_read (중복 통과분): 37건
```

즉 **등급 면제(`FLOOR_EXEMPT_GRADE = "must_read"`)는 이 표본에서 한 번도 발동하지 않는다.**
결손 면제가 이미 같은 항목을 전부 통과시키기 때문이다.

### 그래서 무엇이 문제인가

지금은 no-op이지만 **features 결손이 고쳐지는 순간 의미가 생긴다.** 그때 등급 면제는
"`must_read`는 점수와 무관하게 전량 통과"로 남고, 이는 명세 P1(*"채우지 않는다 — 점수의
하한을 신설한다"*)이 `must_read` 전체에 대해 무효가 된다는 뜻이다.

`must_read`가 신뢰할 만한 판정이면 괜찮다. 그런데 §4에서 본 대로 **`must_read`의 40%는
LLM 판정이 아니라 batch 실패 시 출처로 자동 부여된 값**이다. 등급을 점수보다 위에 두는
조항은 그 신뢰도를 전제하는데, 그 전제가 성립하지 않는다.

> **권고: 등급 면제를 빼고 결손 면제만 남긴다.** 표본에서 결과가 동일하고, 명세 P1과
> 충돌하지 않으며, 나중에 결손이 고쳐지면 자동으로 의도대로 작동한다.

---

## 8. 등급 기반 하한 (현재 구현)

`must_read` 무조건 통과 + **features 결손 면제** + `nice_to_know`에만 하한.

| nk 하한 | 국내 0건 회차 | 국내 평균 | 해외 0건 회차 | 해외 평균 | 총 선정 | nk 비중 |
|---:|---:|---:|---:|---:|---:|---:|
| 없음 | 0 | 2.9 | 0 | 5.6 | 169 | 66.3% |
| 12 | 1 | 2.5 | 2 | 5.2 | 154 | 63.0% |
| 13 | 2 | 2.2 | 2 | 5.2 | 148 | 61.5% |
| **14** | **2** | **2.1** | **2** | **4.8** | **139** | **58.3%** |
| 16 | 3 | 1.7 | 4 | 4.3 | 121 | 47.9% |
| `must_read`만 | 11 | 0.6 | 5 | 3.0 | 72 | 0.0% |

`must_read` 탈락률은 정의상 0%다.

현재 `ranking_config.json`에 들어 있는 값:

```json
"selection_floor": {"nice_to_know": {"domestic": 14.0, "overseas": 14.0}}
```

- 20회차 기준 국내 2회·해외 2회가 0건 → **월 4~5회 "조용한 날"**. 명세의 "주 1~2회" 목표와
  같은 수준이다.
- 총 선정 169 → 139건 (−18%)
- 국내·해외를 같은 값으로 둔다 — §1에서 분포가 거의 같기 때문.

## 9. must_read 후보 수 — 컷오프 효과는 국내에서 난다

| 지역 | 회차 평균 | must_read 0건 회차 |
|---|---:|---|
| 국내 | 0.6 | **11/20 (55%)** |
| 해외 | 3.8 | 5/20 (25%) |

회차별 (국내/해외):
`0/4 · 1/1 · 1/7 · 2/10 · 0/4 · 0/0 · 0/2 · 3/2 · 1/10 · 0/0 · 1/0 · 1/13 · 0/0 · 0/3 ·
1/3 · 0/2 · 0/4 · 0/0 · 1/6 · 0/5`

**국내는 절반 이상의 회차에서 `must_read`가 0건인데도 매번 3건이 나갔다.** 그 자리는
`nice_to_know`가 채운 것이다. 명세가 지적한 "캡이 하한처럼 작동한다"의 실체가 이 표다.

---

## 10. Phase 1 임계값 권고

| 명세 항목 | 명세 값 | 권고 | 근거 |
|---|---|---|---|
| `score_threshold_domestic` | 절대값 | **14.0 — 단, `features` 결손 면제 필수** | §7-2 |
| `score_threshold_global` | 절대값 | **14.0 — 국내와 같은 값** | §1 · §7-2 |
| 면제 규칙 | 명세에 없음 | **`features is None` → 통과** (필수) | §7 |
| 등급 면제 | 명세에 없음 | **넣지 말 것** — 표본에서 no-op이고 명세 P1과 충돌 | §7-2 |
| `display_cap_domestic` | 3 | 유지 (`daily_brief.DOMESTIC_CAP`) | 이미 존재 |
| `display_cap_global` | 6 | 유지 (`daily_brief.FOREIGN_CAP`) | 이미 존재 |

> **초판 정정**: 이 문서 초판과 `2026-08-03-selection-floor-backtest.md`는 "절대 점수 하한은
> 채택 불가"라고 썼다. **틀렸다.** 문제는 절대 하한이 아니라 **면제 규칙이 없는 것**이었다.
> 결손 면제를 넣으면 명세 형태 그대로 쓸 수 있고, 현재 구현된 등급 기반 하한과 선정
> 결과가 항목 단위로 동일하다(139/139).

### 배포 후 재조정 기준

회차별 선정 건수를 1주일 관찰한다. `ranking_config.json` 숫자만 고치면 된다.

- 0건인 회차가 주 3회 이상 → 하한이 높다
- 한 번도 0건이 아니다 → 하한이 무의미하다
