# 2026-08-10 제품 감사 — 라운드 1

기준: 라이브 `https://nuclens-v2.pages.dev` (generation `20260810T141837Z`, 이슈 160개,
아카이브 2,271건). 저장소 기준 커밋 `bfe6c60`.

우선순위는 ①실무 워크플로 ②정보 신뢰성 ③리서치 효율 ④사용성 순으로 봤다.

## 고친 것

### NUC-P1-001 — 남의 기사 본문이 '이 기사의 내용'으로 나갔다

`article_body.matches_title()` 이 일반 어휘만으로 통과했다.

- 실사고: 이슈 `한수원, 신규 대형 원전 및 SMR 부지 후보지 선정` 의 `기사 내용` 이
  **해외건설 수주 500억 달러** 이야기였다. 원문 URL(polinews.co.kr)을 직접 받아
  재현했다 — 제목 8토큰 중 겹친 것은 `원전`·`대형` 둘.
- 원인: `max(_RELEVANCE_MIN_HITS=2, round(8*0.30)=2)` 라 짧은 제목에서 하한과
  비율이 정확히 겹친다. 원자력 기사면 어디에나 있는 낱말 둘이 '같은 사건'의
  증거가 됐다. `keei_match`·`issue_review` 에서 이미 두 번 겪은 함정
  ("같은 분야"를 "같은 사건"으로 읽는 것)이 수집 계층에서 재발.
- 보정: 아카이브 직접 URL 최신 51건을 실제로 받아 겹침을 쟀다. 44건이 0.55 이상,
  0.50 미만은 4건. 오탐(0.25)과 갈린다 → `0.50 / 3`.
- 비대칭 근거: 버리면 원래 있던 제목+요약 경로로 돌아간다. 남기면 다른 사건의
  본문이 전문가에게 '이 기사의 내용'으로 제시된다.
- 과거분: 수집기를 고쳐도 아카이브에 쌓인 것은 안 고쳐진다 → `build_data.usable_detail()`
  이 화면으로 나가는 자리에서 한 번 더 본다(빌드마다 전체를 지나가므로 즉시 걷힘).
  실측 237건 중 5건(2.1%) 제외, 해외 65건은 0건.

### NUC-P2-002 — '달라진 것' 라벨 아래 바뀌기 **전** 상태만 있었다 (10/160)

`card_change_display` 가 화살표 문장의 뒤쪽(현재 상태)을 걷어내면 앞쪽만 남는데,
화면은 그 줄에도 `달라진 것` 라벨을 세웠다. 라벨은 변화를 묻고 문장은 옛 상태를
답한다 — 훑어보는 사람이 옛 상태를 오늘 일로 읽는다.

라벨 문자열을 데이터에서 빼고(`직전 브리핑: ` 접두 제거) `change_kind` 를 실어
화면이 고르게 했다(`직전까지`). 지금 상태는 바로 위 제목이 말한다.

### NUC-P2-003 — 빌려 온 요지의 출처가 문단 뒤 작은 글씨였다 (15/39)

`pick_detail` 은 대표 기사에 요지가 없으면 다른 멤버 것을 쓴다(설계대로). 그런데
머리는 `기사 내용`, 출처는 문단 **뒤** `<small>` 이라 이슈 제목의 내용으로 읽힌다.
머리를 `관련 기사 내용` 으로 바꾸고 출처를 문단 앞으로 올렸다.

### NUC-P2-004 — 폰트 한 벌이 첫 로드 전송량의 77%

| | 원본 | 부분집합 |
|---|---|---|
| PretendardVariable.woff2 | 2,057,688 B | **741,516 B** |

첫 로드 실측 5.9MB 중 폰트가 2.0MB. woff2 는 이미 압축돼 있어 엣지 gzip/br 이
더 줄여 주지 않는다(나머지 JSON·JS·CSS 전송량 합계는 60만 바이트대).

24일치 지면 + 화면 문구의 서로 다른 한글 음절은 **896자**, 전부 KS X 1001 안.
2,350자만 남겨 **잃는 글자 0자**(부분집합 cmap 과 전수 대조). `web/tools/subset_font.py`
로 생성하고 산출물을 커밋한다 — CI 에 fontTools 를 넣지 않기 위해서다.

## 라운드 2

### NUC-P2-007 — 발간물 제목이 전부 같은 20자로 시작했다 (17/20)

목록의 모든 행이 `국제원자력기구(IAEA) …` `경제협력개발기구 원자력기구(OECD-NEA) …`
로 시작하는데, 바로 위 줄에 같은 기관 바이라인이 또 선다. 정작 다른 부분이
오른쪽으로 밀려 스캔이 죽는다. NEA 는 제목 표기(`경제협력개발기구 원자력기구(OECD-NEA)`)
와 바이라인(`OECD 원자력기구(NEA)`)이 달라 **두 기관처럼 읽힌다.**

근본 원인은 번역 입력 형식이었다 — `[3] (OECD-NEA) Accelerating SMRs` 처럼 괄호가
제목 바로 앞에 붙어 모델이 그것까지 제목으로 읽었다. 프롬프트에는 "없는 말을 붙이지
않는다"가 이미 있었지만 형식이 그 규칙을 이겼다. 자리를 갈라 준다(`발행기관= | 제목=`).

과거분은 `strip_org_prefix()` 가 빌드에서 걷는다. 판정은 **괄호 안 약자**로 한다 —
한국어 표기는 번역마다 흔들리지만 약자는 안 흔들린다. 17/17 정확히 걷히고,
`소형모듈원자로(SMR) 배치 가속화` 같은 무관한 괄호와 제목이 기관명뿐인 경우는 살린다.
PROMPT_VERSION 은 올리지 않았다 — 표시 계층이 과거분을 이미 고치므로 재번역 호출이
필요 없다.

### 적대적 QA (Agent 12) — 새 P0/P1 없음

- 잘못된 파라미터(`date=9999-99-99` · 없는 `issue` · 없는 `view`)는 전부 최신
  브리핑으로 조용히 물러난다. 없는 `topic` 은 빈 상태 + '필터 해제'를 정확히 띄우고,
  2026-08-01 의 "한 화면이 자기모순" 버그는 재발하지 않았다.
- 검색에 `<script>alert(1)</script>` 를 넣어도 그대로 이스케이프돼 텍스트로 뜬다.
  한글 800자를 넣어도 가로 스크롤 0(375·1440 둘 다).
- 모바일 상세 모달: 가로 넘침 0, 저장은 localStorage 에 남고 버튼이 `저장됨` 으로 바뀐다.
- **타임라인의 기사 링크는 Google News 가 아니라 매체 주소로 직행한다**
  (kwangju.co.kr · ytn.co.kr · polinews.co.kr 실측) — 카드의 `원문` 버튼이 리다이렉트여도
  추적 경로는 살아 있다. NUC-P3-006 의 심각도를 낮춘다.

### 팩트체크 한 건 — 주장은 사실, 서법이 한 칸 세다

홈 '이번 주 결론'의 `원안위는 계속운전 원전의 안전성을 신규 원전 수준으로 확인하고` 를
네이버 뉴스로 대조했다. 연합뉴스·KBS·뉴시스·노컷·한겨레 등 **9개 이상 매체가 같은
사실을 보도**한다(2026-08-04 대통령 업무보고). 주장 자체는 맞다.

다만 원문은 전부 `확인하겠다고 밝혔다` · `확인해 나갈 계획` 인데 Nuclens 제목은
`안전성 확인 발표` 다 — 계획을 완료로 한 칸 올려 읽힌다. 한수원 독자에겐 큰 차이다.

원인을 좇으니 **살아 있는 결함이 아니었다.** 그 기사는 2026-08-04 자로 `article_body`
배포(08-07) 전이라 본문이 없고, 제목만 받은 모델이 한국어 헤드라인의 서법 생략을
그대로 옮긴 것이다. 프롬프트에는 이미 "계획·예정·전망·검토를 완료된 사실처럼 바꾸지
말 것"이 있고 규칙이 깨진 게 아니라 입력이 부실했다. 실제로 그 URL 의 본문을 지금
받아 보면 `확인하겠다고 밝혔다` 가 그대로 들어 있다(v.daum.net, 1,451자, 수집 가능).

**본문 확보율 실측**: 08-08 40% → 08-09 79% → 08-10 74%. `MAX_CURATION_PER_RUN` 과
`MAX_BODY_FETCH_PER_RUN` 이 둘 다 80 이라 큐레이션되는 기사는 전부 본문 시도를 받는다.
따라서 이 사례는 21일 창에서 밀려나며 자연 해소된다 — 손대지 않는다.

## 라운드 3

### NUC-P2-005 — 매체명 59%가 도메인이었다 (해결: 표가 아니라 페이지에게 묻는다)

`hankyung.com` `hidomin.com` 은 한국 독자에게 매체명이 아니다. 아카이브 1,017건 중
601건(59%).

처음엔 도메인→이름 표를 만들려다 접었다 — 꼬리가 **251개 도메인**이고 상위 60개로도
58% 밖에 안 덮인다. 그런데 더 결정적인 것은 **표가 틀린다**는 점이다: `chosun.com`
기사의 실제 매체가 조선일보가 아니라 **조선비즈**였다.

정답은 이미 손에 있었다. 본문 때문에 페이지를 어차피 받고 있으므로 `og:site_name`
은 공짜다. 표본 30건 실측 — 29건이 받아졌고 그중 **25건에서 이름을 얻었다**:
중앙일보·국민일보·한국일보·이데일리·아시아경제·조선비즈·대한경제·강원도민일보·
더팩트·글로벌이코노믹·이투데이·한국보험신문·오늘경제. 못 얻은 4건은 태그가 없는
매체라 종전 표기를 그대로 둔다.

포털 미러도 이걸로 풀린다 — `v.daum.net` 로 표기되던 국내 계속운전 기사 5건은
페이지가 `Daum | 노컷뉴스` 라고 말한다. 포털 접두는 걷어내고 원 매체만 남긴다.

설계 두 가지:
- **본문 판정과 매체명을 갈랐다.** 제목과 안 맞아 본문을 버리는 기사도 카드에는
  실리고, 그 기사의 매체명은 멀쩡하다. `fetch_one(meta=...)` 이 본문 검사를 하기
  **전에** 이름을 담는다.
- **이미 이름이 있으면 절대 안 덮는다.** Google News `<source>` 가 준 `전기신문`
  을 포털 미러의 이름으로 덮으면 원 매체가 지워진다.

**적용 범위는 앞으로 수집되는 기사부터다.** 이미 쌓인 601건은 재크롤이 없어 그대로
남는다 — 백필(601회 요청)은 21일 창 밖 행의 표기를 고치는 일이라 값어치가 낮다고
보고 안 했다. 필요하면 별도 도구로 한 번 돌리면 된다.

### NUC-P2-006 — 원문 링크 24%가 Google News 리다이렉트였다

이슈 160개 중 38개(24%)의 `원문` 이 `news.google.com/rss/articles/…` 다. 브라우저에서는
실제 매체로 넘어가고 타임라인 링크는 원래 매체 주소로 직행하므로 기능은 산다. 문제는
둘이다 — **클릭 전에 어디로 가는지 알 수 없고**, Google News 주소는 시간이 지나면
만료된다. "나중에 다시 찾는다"가 업무인 사람에게 그건 인용이 끊긴다는 뜻이다.

`resolve_google_news()` 는 이미 있었고 실주소를 풀고 있었는데 **그 결과를 버리고
있었다.** 레코드에 `resolved_url` 로 남기고 화면은 `source_url()` 로 읽는다.

`url` 은 절대 안 바꾼다 — 그게 dedup 키(`url_hash`)라 바꾸면 같은 기사가 새 기사로
다시 들어온다. 표시용 주소만 따로 둔다.

실측 3건(NEI 기사):
```
before  https://news.google.com/rss/articles/CBMieEFVX3lxTFBNbnJBZ…
after   https://www.neimagazine.com/news/tae-black-moon-eye-he-3-fusion-fuel
```
**본문 수집이 403 으로 실패해도 주소는 복원된다** — 해석이 본문 요청보다 앞에 있기
때문이다. 페이월·봇차단 매체(Reuters·FT 등)에서 특히 값어치가 있다.

현재 아카이브에는 `resolved_url` 이 없으므로 이번 변경은 기존 데이터에 **무영향**이다
(빌드 산출물 news.json 바이트 동일로 확인). 다음 크롤분부터 붙는다.

## 라운드 4 (2026-08-11)

### NUC-P3-008 — 지도 타일이 나라 이름 대신 코드를 띄웠다

39개 타일 중 둘이 `MX 0건`·`AT 0건`. 나머지 37개는 `브라질 0건` 처럼 한국어다.
나라를 말하는 표가 셋(`COUNTRY_LABELS`·`COUNTRY_GRID`·`COUNTRY_REGION`)인데
뒤의 둘에만 넣어서 화면이 `COUNTRY_LABELS[c] || c` 로 **조용히** 물러났다.

지금은 둘 다 0건이라 툴팁에만 보이지만 멕시코 라구나베르데 기사가 하나만 들어와도
선두 카드 국가 칩이 `MX` 로 뜬다. 값을 채우고 **계약을 테스트로 잠갔다** — 나라를
더할 때마다 재발할 자리다. 격자와 대륙 묶음이 같은 집합인지도 함께 본다(다르면
타일 합과 막대 합이 어긋난다).

### 감사만 하고 손대지 않은 것

**흐름 탭·주간 리포트·국가 지도는 상태가 좋다.** 방향 표시에 문턱을 명시하고
(`8pp 이상·표본 8건 이상일 때만`), 지도는 근사 배치임을 스스로 밝히며, 대륙 분류
밖 6건을 숨기지 않고 적는다. 타일 합 = 대륙 합도 실측으로 맞았다
(북미 59+6=65, 아시아 47+8+5=60, 유럽·러시아 4+7+5+5=21).

### 📱 결정이 필요한 것 — 모바일 첫 화면을 주간 요약이 차지한다

| 뷰포트 | `오늘 3분` 높이 | 선두 이슈 위치 | 화면 수 |
|---|---|---|---|
| 1440×900 | 296px | 733px | 0.8 |
| 1024×768 | 318px | 748px | 1.0 |
| 768×1024 | 408px | 838px | 0.8 |
| **375×812** | **700px** | **1,105px** | **1.36** |

모바일만 이상치다. 글이 좁은 폭에서 접히며 블록이 296→700px 로 두 배 넘게 불고,
선두 이슈가 1.36 화면 아래로 밀린다. 첫 이슈 **행**은 1,719px(2.1 화면).

내용도 짚어 둔다 — 블록 제목은 `오늘 3분` 인데 안쪽 라벨은 `이번 주 결론` 이고,
**요일과 무관하게 매일 같은 주간 문장이 뜬다**(8/5·8/7 실측 동일). 즉 탭은 '오늘'을
약속하는데 첫 1,100px 은 '이번 주'를 답한다.

다만 코드 주석이 이유를 적어 두고 있다 — `다음 확인` 은 카드의 `open_question` 이
실측 19건 중 0건이라 **주간 watchpoints 가 화면에서 그 질문에 답하는 유일한 자리**다.
그래서 이건 버그가 아니라 편집 우선순위 문제이고, 이 저장소가 첫 화면 개편으로
세 번 되돌아온 이력이 있어 **임의로 뒤집지 않고 남긴다.**

**결정(2026-08-11): ①순서만 바꾼다.** 좁은 화면(≤767px)에서 `오늘 3분` 을 선두
이슈 **아래**로 옮긴다. 내용은 하나도 안 숨긴다 — 주간 watchpoints 는 카드의
`open_question` 이 비어 있어 화면에서 그 질문에 답하는 유일한 자리라, 접으면
모바일 사용자에게선 사실상 사라진다.

결과(라이브 실측): 선두 카드 **1,105 → 362px**. 데스크톱은 693px 로 그대로.

게토차 둘:
- **자리 잡기를 `renderTodayAgenda` 안에서 부르면 안 된다** — 그 시점엔
  `leadIssue.hidden` 이 아직 이전 상태라 조건이 늘 거짓이다(그렇게 넣었다가
  자리가 안 바뀌어 한 번 헛돌았다). 표시 여부가 정해진 **뒤에** 부른다.
- 767px 경계 이벤트를 같이 걸지 않으면 리사이즈한 사람만 어긋난 채 본다.
  양방향(375→1200→375) 실측 확인.

### 흐름 탭 `투자 테마 강약` → `주제별 강약` (사용자 결정)

한수원 임직원용 서비스가 투자 시그널을 주는 모양새는 기획 단계부터 걸려 있던
우려다. 담는 내용(`theme_moves`)은 그대로 두고 프레이밍만 중화했다 — 실제로 뜨는
이름은 SMR·계속운전·전력수요처럼 주제어이지 종목이 아니다.

**남은 같은 문자열**: `weekly_bot.py:351` 의 텔레그램 머리말 `💰 투자 테마 강약`
과 그것을 잠그는 `tests/test_weekly_metrics.py:96`. 같은 독자에게 가는 다른 표면이라
같이 볼 값어치가 있지만 이번 결정의 범위(흐름 탭) 밖이라 손대지 않았다.

## 라운드 5 (2026-08-11) — 배포를 막고 있던 두 데이터 조건

`deploy-web.yml` 은 `build_data.py` 로 데이터를 구운 **뒤에** 테스트를 돌리고
그 다음에 배포한다. 그래서 데이터 조건이 테스트로 살아 있으면 **뉴스가 어떻게
쌓였는지만으로 CSS 오타 수정까지 배포가 막힌다.** 발견 시점에 실제로 그 상태였다.

### NUC-P1-009 — 주별 균형 게이트에 skipIf 표식이 빠져 있었다

실데이터 주별 합계 [56, 50, 101] = **2.02**, 상한 2 초과 → CI 레드.
`test_tracking_rate_meets_target` 은 2026-08-03 에 똑같은 이유로 이미 꺼졌고
워크플로 주석이 그 사고를 적어 두고 있는데, 이 검사만 표식이 없었다.

화면은 이 왜곡에 이미 견딘다 — 주별 **비중**으로 정규화하고 `8pp 이상·표본 8건
이상`일 때만 방향을 말한다. 그래서 값이 넘어도 거짓 방향은 안 뜬다. 배포 경로에서만
끄고 로컬에서는 켜 둔다(데이터 품질을 보는 자리는 그대로 유지).

### NUC-P1-010 — 나라 오태깅 한 건이 클러스터 게이트를 막았다

IAEA 주최 국제 논의 기사(dt.co.kr)에 `KR` 이 붙어 있었다. 요약에 한국 행위자가
없는데도. 같은 사건을 다룬 미국 기사(`267f7580`)와 한 이슈로 묶이자 `{KR}` vs
`{US}` 가 다리 없이 갈라져 국경 충돌로 잡혔다.

**병합은 옳고 태그가 틀렸다.** 규칙을 풀면 과병합이 조용히 돌아온다(2026-08-05
팍스 사고에서 이미 배웠다) — 판정을 고친다. `archive_repairs.json` 에 `countries`
수선을 넣고 `infer_countries` 가 맨 앞에서 읽는다. 실측 1,037건 중 `GLOBAL+KR`
동시 태그는 **2건**뿐이라 드문 오판이고, 그 목적의 파일이 이미 있다.

태그 하나가 세 군데를 동시에 망가뜨리고 있었다: ①지역이 국내가 되어 국내 풀에서
경쟁 ②국가 지도의 한국 칸을 부풀림 ③배포 게이트 차단.

**딸린 게토차**: 수선해도 지역이 국내로 남았다. `infer_region` 이 `scope` 를 맨
앞에서 보는데 그 필드는 큐레이션이 명시할 때만 채워진다(실측 157건 중 148건 None,
이 저장소가 이미 "신뢰 불가"로 박제한 필드다). 사람이 고친 나라가 옛 `scope` 하나에
다시 덮이고 있었다 → `manual-repair` 면 scope 를 건너뛴다.

아카이브는 다시 쓰지 않는다(`--migrate-quality` 는 파일을 통째로 갈아엎는 유지보수
명령이다). 빌드가 매번 전체를 지나가므로 과거분까지 즉시 반영된다.

**프로덕션 확인**: `countries: ["GLOBAL"] · region: 해외 · region_source: countries`,
KR 태그 기사 607 → 606. **캐시버스터 없이 받으면 옛 값이 그대로 온다** — 이 저장소가
이미 아는 함정인데 또 밟았다. `?cb=` 를 붙일 것.

## 진행 점검 (2026-08-11 기준)

### 파이프라인 실측 — 앞선 라운드가 실제로 붙었나

크롤 3회분 이후 아카이브 2,173건 기준: `site_name` **155건**, `resolved_url` **70건**.
복원 표본 — `chosun.com → 주간조선`(조선일보가 아니다. 손으로 만든 도메인 표였으면
틀렸을 자리), `dnews.co.kr → 대한경제`, `kjmbc.co.kr → 광주MBC`,
`newspim.com → 뉴스핌`. 실주소는 latribune.fr·**reuters.com** 까지 복원됐다 —
Reuters 는 `BLOCKED_DOMAINS` 라 본문을 아예 안 받는데도 주소가 나왔다(해석이 본문
요청보다 앞이라는 설계가 실제로 값을 냈다).

라이브 지표: 호스트명 매체 59% → **55%**, Google News 링크 24% → **17%**.
재크롤이 없는 과거분은 그대로이므로 21일 창이 돌면서 더 내려간다.

**CI 배포가 스스로 돌았다** — 라이브 generation 이 `20260811T104523Z`(10:45 UTC
크롤분)로, 내 수동 배포가 아니라 파이프라인 산출물이다. 라운드 5 의 게이트 해제가
실제로 배포를 되살린 것이 확인됐다.

### 커버리지 — 원 지시의 12개 관점 대비

| 관점 | 상태 | 근거 |
|---|---|---|
| 04 사실 검증 | **깊게** | 잘못된 본문 규명·수리, 주장 1건 9개 매체 대조, 서법 드리프트 추적 |
| 05 데이터 파이프라인 | **깊게** | matches_title·요지 차용·매체명·실주소·나라 태그 |
| 08 프런트엔드 | **깊게** | 콘솔·네트워크·DOM 순서·파스 게이트 |
| 12 레드팀 | **깊게** | 잘못된 파라미터·긴 입력·주입·빈 상태·모바일 모달 |
| 01 제품 총괄 | 중간 | 화면 4개 평가, IA 불일치(모바일 주간 우선) 발견·수정 |
| 06 검색·발견 | 중간 | 검색·딥링크·엔티티 제안 확인. **랭킹 품질은 미평가** |
| 07 UX | 중간 | 첫 화면 밀도·라벨·목록 스캔성. 전면 시각 감사는 아님 |
| 09 성능·모바일 | 중간 | 폰트 −64%·모바일 순서. **접근성 스윕은 이번에 안 함** |
| 02 기자 워크플로 | 얕음 | 시나리오 A·F 만 실행 |
| 03 정책 리서치 | 얕음 | 보고서 탭 확인. **시나리오 C·D 미실행** |
| 10 보안·신뢰성 | 얕음 | noindex·번들 비밀값 0·XSS 이스케이프 확인. **CSP·X-Frame 부재**(P3) |
| 11 경쟁 조사 | **미실시** | — |

### 시나리오

- **A 아침 브리핑** — 실행·측정(모바일 선두 이슈 1,105 → 362px)
- **F 회의적 검증** — 실행. 이 경로에서 잘못된 본문을 찾았다
- **B 속보→원문** — 부분(추적 경로 확인, 실주소 배선)
- **E 주제 모니터링** — 부분(딥링크·'지난 확인 이후' 확인)
- **C 기사 작성 / D 정책 브리핑** — **미실행**

## 라운드 7 (2026-08-11) — 시나리오 D 를 실제로 수행

지금까지는 화면을 검사했지 업무를 해보지 않았다. "고리 3·4호기 계속운전 심사
현황으로 정책 브리핑 자료를 만든다"를 Nuclens 만으로 끝까지 해봤다.

### NUC-P1-011 — 검색이 자연스러운 두 낱말에 0건을 냈다

첫 단계에서 막혔다. `원안위 계속운전` → **0건**. 그런데 `계속운전` → 11건.

원인: 검색이 이어 붙인 텍스트 **한 덩어리에 대한 substring** 이었다. 제목은
`원안위, 계속운전 원전의…` 라 **쉼표 하나**에 막힌 것이다.

```
계속운전                 11건
계속운전 심사             3건
원안위 계속운전            0건   ← 두 낱말이 다 있는데도
```

검색에서 0건은 "그런 이슈가 없다"로 읽힌다. 랭킹이 나쁜 것보다 나쁘다 —
**거짓 음성이 권위 있게 보이고**, 브리핑 쓰는 사람은 없는 줄 알고 넘어간다.

덤으로 드러난 것: 드롭다운(`searchNormalize`)과 결과 페이지(`normalizedSearch`)가
**서로 다른 정규화**를 쓰고 있어 같은 질의가 두 곳에서 다르게 나왔다.

수리: 질의를 낱말로 쪼개 각각 찾는다(AND 유지 — OR 로 풀면 잡음). 정규화는 한
벌로 통일. 하이라이트도 낱말별로. 실측 전후 — `원안위 계속운전` 0→3,
`smr 규제` 0→19, `고리 계속운전` 0→2. `고리 34호기` 가 `고리 3·4호기` 를 찾는 것은 덤.
테스트는 app.js 에서 순수 함수를 뽑아 **node 로 실제 실행**한다(문자열 계약이 아니라 동작).

### 나머지 단계는 잘 굴러갔다

이슈 상세에 `한 줄 결론 / 왜 중요한가 / 시사점 / 검증 상태 / 사건 타임라인`이 다
있고, **관련 이슈** 블록이 나머지 계속운전 이슈 3건으로 다리를 놔 준다(그중 하나는
근거 12건). `자료 팩 복사`는 제목·메타·결론·검증·타임라인·역링크를 담은 마크다운을
제대로 만든다.

### 남은 두 개 — 팩의 값어치를 깎는 것

**① 인용이 인용답지 않다.** 실제로 복사한 팩의 출처 줄:

```
- 8월 4일 · 원안위, 계속운전 원전의 … (v.daum.net)
  https://news.google.com/rss/articles/CBMiT0FVX3lxTE5hekxXbS1ZNC1o…
```

포털 호스트명이 매체명 자리에 있고 링크는 리다이렉트다. 둘 다 이번 세션에
**앞으로는** 고쳤지만 이미 쌓인 기록은 그대로다. **실측: 표시 기사 1,136건 중
777건이 해당하고 그 가운데 645건이 최근 7일분이다** — 즉 지금 사람들이 읽는
구간의 대부분이다. 라운드 3 에서 "21일 창 밖 행이라 값어치 낮다"고 판단한 것은
틀렸다. 백필이 필요하다.

**② 팩이 이슈 하나씩만 나온다.** 브리핑은 여러 이슈를 묶는데 `자료 팩 복사`는
개별이고, 저장 목록에 묶음 내보내기가 없다. 관련 이슈 3건을 세 번 복사해 손으로
붙여야 한다.

## 남은 것 (다음 라운드)
- **모바일 첫 이슈까지 1,596px** (375×812 기준, 약 2화면). 8/10 이 월요일이라
  주간호가 히어로를 차지한 날의 값이다 — 평일에 다시 재고 판단할 것.
- **주별 표본 균형 게이트**(`test_live_data_weeks_are_within_the_front_end_gate`)가
  로컬 빌드에서 실패한다: 주별 합계 [56, 50, 101], 비 2.02 > 상한 2. 코드가 아니라
  데이터 조건이고 이번 변경과 무관하다(주 집계 경로 미수정).

## 검증

- 봇 481 / 웹 358 테스트. 남는 실패는 위 주별 균형 게이트 1건뿐.
- 로컬 빌드로 실렌더 확인: 부분집합 폰트만 내려받음(원본 요청 0), 두부 0,
  8/8 브리핑에서 `직전까지` 3건 / `달라진 것` 1건이 의도대로 갈렸다.
- `한수원 … 부지 후보지 선정` 의 잘못된 요지가 `news.json`·`issues.json` 양쪽에서
  사라진 것을 재빌드로 확인.

## 라운드 8 (2026-08-11) — 백필과, 라운드 1 진단의 정정

### 인용 백필 — 시나리오 D 의 산출물이 인용 가능해졌다

`tools/backfill_sources.py` 로 799건을 받아 매체명 610 / 실주소 205 확보.
사이드카(`archive_source_backfill.json`)에만 적고 **아카이브는 안 건드린다**.

| | 전 | 후 |
|---|---|---|
| 호스트명이 매체명 | 619/1136 (55%) | **78/1136 (7%)** |
| Google News 리다이렉트 | 191/1136 (17%) | **0** |

시나리오 D 팩의 그 줄: `v.daum.net + news.google.com/rss/…`
→ `노컷뉴스 + v.daum.net/v/20260804162700912`.

### 🔴 라운드 1 진단이 반쪽이었다 — 틀린 것은 요지가 아니라 제목이었다

폴리뉴스 기사에서 나는 "본문을 잘못 긁어왔다"로 보고 **요지를 지웠다**. 그런데
아카이브 원본을 보면 반대였다:

```
원 제목  : 해외건설 500억 달러 시대 겨냥…K건설, 중동 플랜트서 원전·전력 선회
title_kr : 한수원, 신규 대형 원전 및 SMR 부지 후보지 선정
summary  : 한국수력원자력이 신규 대형 원전 2기 후보지로 경북 영덕군을,
           SMR 후보지로 부산 기장군을 선정했다고 밝혔다.   ← 원문에 없는 사실
importance: must_read
```

본문(해외건설)은 **맞았고** 제목·요약이 통째로 날조된 것이다. 내 게이트는 요지와
제목의 겹침만 보고 요지를 버렸다 — **거짓말은 남기고 진실을 지운 셈**이다.
(사람이 `selection_overrides.json` 으로 직접 내렸고, 그 기록을 보고 규명했다.)

겹침만으로는 어느 쪽이 틀렸는지 못 가른다. 대신 **원 제목을 나란히 찍어 사람에게
넘긴다**. 실측 1,136건 중 불일치 6건이고 그중 must_read 1건이 정확히 이 기사다 —
나머지 5건은 정상 의역(사설·시황 제목을 사건 제목으로 다시 씀). 신호 대 잡음이
쓸 만하다.

**교훈: 두 신호가 어긋날 때 "어느 쪽이 원본에 가까운가"를 먼저 정하고 버릴 것.
자동으로 한쪽을 버리면 반대 경우에 정확히 거꾸로 작동한다.**

### 편집 도구가 배포를 막고 있었다

`selection_overrides.json` 은 문서가 권하는 편집 도구인데, 검사 둘이 그 사용을
금지하고 있었다 — 항목이 하나라도 들어오면 실패(`_is_valid_and_empty`), 발송분과
화면분 1:1 강제(16 != 17). 사람이 날조 카드를 내린 그 순간 CI 가 빨개졌다.
라운드 5 에서 데이터 조건 둘을 걷어냈는데, 이번 것은 **사람의 편집 행위**를
막고 있었다는 점에서 더 나쁘다.

## 라운드 9 (2026-08-11) — 묶음 자료 팩

시나리오 D 의 마지막 구멍. 계속운전 하나를 좇는데 관련 이슈가 3건인데 `자료 팩
복사` 는 하나씩만 나온다 — 세 번 복사해 손으로 붙이는 동안 순서·중복·출처가
흐트러진다.

저장 목록 머리에 `전체 자료 팩 복사`. 머리말(건수·기준일·목차) + 각 이슈 팩을
`---` 로 잇고 최신순. **조립만 한다** — 각 이슈 본문은 `issueMaterialPack` 그대로다.
여기서 형식을 새로 지으면 단건 팩과 두 벌이 되고 금방 갈라진다.

게토차: 살아 있는 이슈가 있을 때만 버튼을 낸다(묘비만 남은 목록에서 누르면 빈
문서가 복사된다).

**검증 요령**: 헤드리스에서 `navigator.clipboard.writeText` 오버라이드가 안 잡히는
경우가 있다(격리 컨텍스트·사용자 활성화). 클릭이 위임까지 닿는 것은 body 캡처
리스너로 따로 확인하고, **조립 자체는 app.js 에서 함수만 뽑아 node 로 돌린다** —
검색 매처와 같은 방식이고 문자열 계약이 아니라 동작을 본다.

## 라운드 10 (2026-08-11) — 사용자 지적: "업데이트 지연"

### NUC-P1-012 — '마지막 수집'이 브리핑 시각을 쓰고 있었다

21:49 KST 에 화면이 `마지막 수집 07:05`. 실제 수집은 **20:35**, 데이터도 최신.
14.7시간 밀린 것처럼 읽힌다.

원인: 화면이 `last_success_at` 을 썼는데 그 필드는 `build_data` 가 주석에 못 박아
둔 대로 **"마지막 정상 브리핑"** 시각이다(하루 1회, 07:00 무렵). 수집은 매시간이라
오후 내내 아침 시각이 떠 있다. **라벨은 '수집'이라 말하고 값은 '브리핑'** — 이번
감사에서 반복해 나온 그 어긋남이다(달라진 것/직전까지, 기사 내용/관련 기사 내용,
오늘 3분/이번 주 결론).

`status.json` 은 `collector_stamp` 를 **이미 싣고 있었는데 화면이 한 번도 안 읽고
있었다.** 그걸 쓰면 끝난다 — 21:37 로 바뀐다.

함께: 오류·중지 문구의 `마지막 정상 수집` → `마지막 정상 브리핑`. 상태
다이얼로그는 **둘 다** 보인다 — 주기가 다른 두 값이라 하나만 두면 나머지가
어긋나도 모른다.

### '업데이트 지연' 자체는 규칙대로다

`briefingStaleDays() > 0` 일 때 뜬다 — `latest_briefing_date` 가 KST 오늘보다
이전이고, 하루 차이면 `STALE_GRACE_HOUR = 9` 시까지는 유예. 즉 **매일 09:00 KST
부터 그날 브리핑이 나올 때까지** 정상적으로 뜬다. 오늘(8/11) 브리핑은 있으므로
지금은 `정상`이다.

### 🔴 게토차 재확인 — browse restart 만으로는 app.js 캐시가 안 풀린다

배포 직후 라이브가 계속 `07:05` 여서 배포 실패로 의심했는데, 엣지는 새 app.js 를
주고 있었다(`curl` 로 `collector_stamp` 2건 확인). 브라우저가 옛 스크립트를 물고
있었던 것이다. `restart` 뒤에도 남았고 **`location.reload(true)` 로 풀렸다.**
이 저장소가 이미 아는 함정인데 진단을 한 번 헛돌게 했다.

## 라운드 11 (2026-08-11) — 진입 경로 일관성·근거 표현·키보드·모바일

기준: 라이브 `https://nuclens-v2.pages.dev` 데스크톱 1280×720, 모바일 375×812,
통합 검색·탐색·이슈 상세·푸터·키보드 우회 경로. 정보 표본은 최신 브리핑과
아카이브 원문을 대조했다.

### NUC-P1-013 — 같은 이슈 ID가 진입 경로에 따라 다른 근거를 보였다

`issue-9e7a3003708adbaa`를 오늘 화면의 통합 검색에서 열면 누적 1건·단일 출처,
탐색에서 열면 누적 5건·공식 원문 1건이었다. URL의 `view=search`만 달랐다.

원인: `currentIssueById`가 오늘 화면에서는 날짜별 브리핑 스냅샷을 우선하고,
탐색에서는 누적 카탈로그를 우선했다. 최신 날짜는 카탈로그를 단일 정본으로 쓰고,
과거 날짜만 당시 스냅샷을 보존한다. 최신 카드도 같은 정본으로 바꿔 카드의
`타임라인 1`과 상세의 `근거 5건`이 한 화면에서 충돌하지 않게 했다.

### NUC-P1-014 — 출처 수가 사실 교차검증으로 과장됐다

한 이슈 타임라인은 같은 주장뿐 아니라 관련 후속 사건도 품는다. 따라서 출처가
둘이라는 사실만으로 `복수 출처 확인`, `독립 출처가 일치`라고 말할 수 없다.
상태 계산은 유지하되 표시를 관찰 가능한 사실로 제한했다.

- `공식 확인` → `공식 원문 포함`
- `복수 출처 확인` → `독립 출처 2곳+`
- 도움말도 `일치`가 아니라 이슈에 `연결`됐다고 설명

### NUC-P1-015 — 원문과 무관한 후보지 선정 단정이 검색에 남아 있었다

라운드 8은 폴리뉴스 기사에서 어긋난 `detail`만 숨겼다. 그러나 더 근본적인 오염은
원문(해외건설·원전/전력 수주 전략)과 무관한 `영덕 대형 원전·기장 SMR 후보지 선정`
제목·요약이었다. `archive/2026-08.jsonl`과 `curated.json`을 원문에 맞춰 함께
교정하고 중요도를 `must_read`에서 `nice_to_know`로 내렸다. 빌드 로그의
`must_read 요지↔제목 불일치`는 1건에서 0건이 됐다.

### NUC-P1-016 — 본문 바로가기가 반복 헤더를 건너뛰지 못했다

첫 링크를 Enter로 실행해도 포커스·스크롤이 링크에 남았다. `main`을 프로그래밍
포커스 대상으로 만들고 클릭 시 본문 포커스와 이동을 명시했다. 이제 키보드 사용자는
탭·검색·상태·저장·테마 컨트롤을 매번 통과하지 않아도 된다.

### NUC-P2-017 — 모바일 고정 탭이 푸터 상태 문구를 전부 가렸다

375×812에서 고정 탭 y=752.5–812.5, 마지막 상태 문구 y=779.1–800.4로 21.3px가
완전히 겹쳤다. 푸터 배경은 화면 끝까지 유지하되 세로·가로 모드 모두 탭 높이와
safe-area만큼 콘텐츠 여백을 확보했다.

### 검증·잔여 위험

- `web/build_data.py` 성공. 교정 뒤 must-read 제목 불일치 0건.
- 웹 테스트 399개 통과(배포 플래그, 3개 스킵), 수집·브리핑 테스트 505개 통과.
- `node --check web/public/app.js` 통과.
- 로컬 브라우저에서 최신 선두 카드 `타임라인 5`와 상세 `근거 5건` 일치,
  근거 배지 새 문구 확인, 콘솔 오류 없음.
- P0/P1 잔여 0. P2 후속: 초기 JSON 선행 로드·단일 핵심 JSON 실패의 전체 화면
  중단·평문 HTTP 원문 26건은 별도 성능/복원력/링크 정비 라운드가 필요하다.

품질 점수(10점): 정확성 9.0, 출처 투명성 9.0, 일관성 9.2, 접근성 8.7,
모바일 안정성 8.8, 성능 6.5, 장애 복원력 6.5. P0/P1을 해소했고 P2는 범위와
재현 근거를 기록했다.
