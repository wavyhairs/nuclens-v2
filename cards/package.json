{
  "name": "nuclens-cards",
  "version": "1.0.0",
  "description": "carousel-lite (tenfoldmarc) fork - Korean patch. runtime renderer.",
  "private": true,
  "scripts": {
    "build": "node build.js"
  },
  "dependencies": {
    "puppeteer": "^24.15.0"
  }
}
