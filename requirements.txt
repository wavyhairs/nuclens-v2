requests>=2.31.0
google-genai>=1.0.0
feedparser>=6.0.0
# 아침 알림 발송(tools/push_notify.py). VAPID 서명·RFC 8291 본문 암호화를 전부
# 맡는다 — py-vapid·http-ece·cryptography 를 함께 끌고 온다. 그 무게를 받아들인
# 이유는 엣지에서 손으로 서명하던 판이 본문을 실을 수 없었기 때문이다
# (functions/push/_shared.js 머리말).
pywebpush>=2.0
